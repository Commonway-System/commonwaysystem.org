# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Commands

```sh
pnpm install       # NOT npm install, see below
pnpm run dev        # dev server, usually http://localhost:5173
pnpm run build      # static build to build/ (HTML/CSS/JS, no server needed at runtime)
pnpm run preview    # serve build/ locally to sanity-check the static output
pnpm run check      # svelte-kit sync && svelte-check --tsconfig ./tsconfig.json
```

**Package manager is pnpm, not npm**, despite what `README.md` says: `pnpm-lock.yaml` and `pnpm-workspace.yaml` exist, there's no `package-lock.json`. The `npm run <script>` commands above are fine, they just invoke `package.json` scripts against the existing `node_modules`. But running plain `npm install` to add or update a dependency tries to re-resolve the entire tree with npm's own resolver against a pnpm lockfile, which is both wrong and extremely slow (minutes, not seconds, in testing). Use `pnpm install` for any dependency change.

There is no test suite and no lint script configured. `pnpm run check` (svelte-check) is the only automated verification step; run it after any change to `.svelte` files or theme TypeScript. It does not catch visual regressions, always check UI changes in an actual browser (see "Working conventions" below). It also does not catch a broken internal link, since that's a prerender-time check: `pnpm run build` fails outright on any same-page `#id` link or route with no matching target, thanks to `adapter-static`'s `strict: true` (see Citation.svelte's note below) — run a real build, not just `check`, after adding pages with a lot of cross-links (a pattern index, a typology-index page, etc.).

**`pnpm add`/`pnpm install` can fail with `ERR_PNPM_IGNORED_BUILDS`** if any dependency (including a transitive one, e.g. `sharp` pulled in by SveltePress's image plugin) has a native build script pnpm's default policy won't run without approval. `pnpm approve-builds` is the normal fix but needs a TTY, unavailable in a non-interactive session; the actual fix is `pnpm-workspace.yaml`'s `allowBuilds:` map (`allowBuilds: { sharp: true }`, one entry per package name) — the non-interactive equivalent, and what's committed here. If this error resurfaces for a new dependency, add its name there rather than fighting the interactive prompt.

## Architecture

This is a [SveltePress](https://sveltepress.site/) site (SvelteKit + a SveltePress theme plugin) with a **fully custom theme** — not the SveltePress default theme reskinned, but built from scratch to keep only its layout shell (sticky navbar, collapsible sidebar, on-this-page rail, prev/next footer) and replace all branding, components, and styles with Commonway System (CS) ones.

**Theme registration flow:** `vite.config.ts` calls `commonwayTheme({...})` (defined in [src/lib/theme/index.ts](src/lib/theme/index.ts)) and passes the result into the `sveltepress()` Vite plugin. `commonwayTheme` is the SveltePress theme contract: it points to `GlobalLayout.svelte` / `PageLayout.svelte` as the layout components, registers the Shiki highlighter, and wires remark/rehype plugins (`remark-gfm`, `remark-directive` for admonitions, `rehype-slug` + a custom heading-anchor plugin, `externalLinks` for automatic external-link handling, `tableScroll` for the scrollable-table wrapper, see below). Theme options (site title, nav, sidebar, GitHub link, edit-link template, footer note, Google Analytics ID) are passed through a virtual Vite module (`virtual:commonway/options`, see `optionsPlugin` in `index.ts`) so theme components can import them without prop-drilling.

**External links open in a new tab, `nofollow`, automatically, everywhere.** `src/lib/theme/markdown/external-links.ts` wires a `rehype-external-links` plugin into the rehype pipeline above: any link in `+page.md` content (citations, prose links, image-links) pointing at a different hostname than `SITE_URL` gets `target="_blank" rel="nofollow noopener noreferrer"` plus an appended icon and `.cw-sr-only` "(opens in new tab)" text, no per-link markdown authoring needed. The same `isExternalHref`/`externalLinkAttrs` helpers (`src/lib/site.ts`) are applied by hand in the few places the theme renders a hardcoded link from data rather than markdown (`Navbar.svelte`'s nav links and GitHub icon link, `PageLayout.svelte`'s edit link, `SidebarGroup.svelte`). **One real gap, confirmed by reading SveltePress's own bundled source:** a raw inline `<a href="...">` HTML tag typed directly into a `.md` file's body bypasses the rehype pipeline entirely and never gets auto-patched (SveltePress runs `remark-rehype` with `allowDangerousHtml: true` but no `rehype-raw`, so raw HTML stays an opaque string node no rehype plugin can see). Adding `rehype-raw` to close this was deliberately **not** done: it would also reparse every existing `<PatternCard>`/`<DensityChip>`/`<Citation>` component tag through an HTML5 parser that lowercases unrecognized tag names, breaking `<PatternCard>` into `<patterncard>` across every pattern page. **Always write links in `.md` content as `[text](url)`, never as a raw `<a>` tag.**

**Wide tables scroll horizontally in their own box instead of overflowing the page.** `src/lib/theme/markdown/table-scroll.ts` (a rehype plugin) wraps every markdown `<table>` in `.cw-table-wrap > .cw-table-scroll > table` plus a small hidden-by-default "scroll for more" hint, at build time — no per-table authoring needed, this applies to every table on every page automatically. `base.css`'s `.cw-table-*` rules add a CSS-only scroll-shadow (the Lea Verou two-gradient trick, no JS) and a themed thin scrollbar to `.cw-table-scroll`; `src/lib/theme/layout.ts`'s `initTableScrollHints()` (called from `GlobalLayout.svelte` on mount and after navigation) measures each table and only shows the hint text when a table actually overflows. Deliberately a wrapper div, not CSS `display: block`/`overflow-x` on the `<table>` element itself (an earlier version of this fix): forcing a table's own computed `display` off `table` strips its implicit ARIA table/row/cell roles in some browsers, breaking screen-reader table semantics — the wrapper keeps the table's native display untouched. A simple 2-column table still renders at full width with no scrollbar; only a table wide enough to need it (many columns, or long unbreakable content) triggers the scroll box.

**`vite.config.ts` is the single source of truth for site content structure** — navbar links, the manual sidebar tree (keyed by route prefix, e.g. everything under `/guide/` uses the `sidebar['/guide/']` entry), GitHub link, edit-link template, site title/description. This is the file to edit for day-to-day content/nav work; `src/lib/theme/` should rarely need touching once the visual direction is set. The sidebar shown for any page is whichever key in `sidebar` the current URL starts with (same model as SveltePress's own default theme). There is no auto-generated sidebar from the routes folder — it's manual by design, since it needs to map onto the Functional Classification / density-tier hierarchy rather than a generic path tree.

**Top-level site structure (post sitewide-restructure, Retrofits added 2026.09.03, Media added 2026.09.04): Home / Guide / Patterns / Retrofits / Media / Reference / Blog / About**, matching the navbar array in `vite.config.ts` in that order. `/media/` (`src/routes/media/`) is the Media Gallery; see the "Media Gallery & Related Media" note below — like `/references/` and `/blog/`, it's a single top-level page with no `sidebar` entry of its own, but unlike either of those it does not use the sitewide 3-column shell (see that note for the custom 2-column layout). `/retrofits/` (`src/routes/retrofits/`) holds the Retrofit Strategy catalog; see the "Retrofit Strategies Batch 2" note below for its structure. `/guide/` (`src/routes/guide/`) holds 14 pages now (guide section rebuild, 2026.08.30; Pattern IDs split out 2026.09.01; Design Speed & Speed Limits added 2026.09.02; Retrofit Strategies Batch 1 scaffolding added 2026.09.03, see below): the bare-root intro (`src/routes/guide/+page.md`, was `guide/introduction/`), grouped under 3 named sections in `sidebar['/guide/']` — **Foundations** (How to Read a Pattern, **How to read a retrofit strategy**, **Pattern IDs**, Density Tiers, Street Types & Classifications, Scale Hierarchy, and **Design Speed & Speed Limits** at `guide/design-speed/`, the last one covering why design speed/operating speed/posted limit diverge and CS's own design-speed-equals-posted-speed rule by Character Type, split out of the intro page since it's foundational vocabulary in its own right), **Find Your Pattern**, and **Make It Happen** (Find Your Pattern is still one placeholder page, a planned diagnostic; Make It Happen now holds 6 real pages, see below). The intro page itself is listed as an ungrouped entry at the top of the array (`{ title: 'Start Here', to: '/guide/' }`, no `items`), same idea as `/about/`'s own ungrouped-at-the-top Overview item, and its sidebar/nav label is "Start Here" (distinct from its own frontmatter `title: Introduction`, which drives the page's actual `<h1>`) specifically to avoid colliding with `/about/commonway-system/`'s "The Commonway System" label. `/patterns/` (`src/routes/patterns/`) holds the bare-root Pattern Index page plus every live pattern page across all nine Scale prefixes (106 as of the 2026.08.31 Network/Corridor/Facility/Element batch, 105 after the 2026.09.02 Commercial Arterial → Stroad merge retired `ART-CAR-07` — check `release-history`'s own live `patternPageCount` computation for the current true count rather than trusting either number as still current): `src/routes/patterns/{network,corridors,local-streets,collectors,arterials,freeways,intersections,facilities,elements}/{slug}/` — its own `sidebar['/patterns/']` entry has 9 groups in that same order, matching the Pattern Index's own section order (see the Pattern ID scheme note below for the full count breakdown). `PageNav.svelte`'s prev/next pagination is derived entirely from whichever `sidebar[...]` list matches the current URL prefix, not hardcoded per page or derived from file-tree order, so moving pages between sidebar sections was enough on its own to fix pagination, no separate per-page edits needed. `/references/` and `/blog/` are new, each a single top-level page with no sidebar entry of its own (an empty-but-present sidebar column is expected on those two, same as it would be on any route matching no `sidebar` key). The Blog index (`src/routes/blog/+page.md`) is not hand-maintained: it renders `<BlogIndex />` (`src/lib/theme/components/BlogIndex.svelte`), which globs `src/routes/blog/*/+page.md` itself via `src/lib/theme/blog.ts` and sorts by each post's `date` frontmatter field, newest first — a new post just needs a `date: YYYY-MM-DD` in its frontmatter to show up correctly ordered. `blog.ts` deliberately duplicates `$lib/server/content.ts`'s glob-and-parse-frontmatter approach rather than importing it, since `BlogIndex.svelte` is a page-rendered component and SvelteKit's build-time guard rejects any client-reachable code importing from `$lib/server/*`, even on a fully static site where that boundary doesn't functionally matter. `llms.txt`'s `SECTION_LABELS` map (`src/routes/llms.txt/+server.ts`) needs a new entry any time a new top-level section like this is added, or that section's pages fall back to showing their raw lowercase route segment as a heading instead of a proper label.

**`/about/`'s sidebar is 3 groups, not one flat list:** About (Overview, The Commonway System, Brand Guide), Development (Release History & Roadmap, Project Governance, Submit a Change, Report a Problem, Acknowledgments), Terms and Policies (Terms and Conditions, Privacy Policy, AI Policy) — each its own `collapsible: false` entry in the `sidebar['/about/']` array in `vite.config.ts`. Three of those pages still have a **display title that no longer matches their URL slug**, a deliberate, scoped rename: `/about/governance/` shows "Project Governance", `/about/contributing/` shows "Submit a Change", `/about/reporting/` shows "Report a Problem" — only each page's frontmatter `title` (which drives both the `<h1>` and the `<title>` tag, see `PageLayout.svelte`) and the sidebar/cross-link label text changed, the route folder names and URLs stayed exactly as they were, specifically to avoid re-triggering a sitewide link audit. The fourth, Acknowledgments, was later moved for real: `/about/credits/` → `/about/acknowledgments/`, route folder and all, so its title and URL slug match again. If either of these two things (the About page's own "In this section" list, or another page's inline prose) names one of these pages by title, use the current title — this bit already once during the original rename, since Contributing/Reporting/Governance/Credits cross-link each other by name in body text, not just via the sidebar.

**Content pages** live in `src/routes/`, one folder per URL segment, each with a `+page.md` (YAML frontmatter + GitHub-flavored markdown). SveltePress lets Svelte run inside markdown, so pages can `<script>` import theme components directly. Admonition blocks (`:::note`, `:::tip`, `:::warning`, `:::danger`, `:::unsourced`) are implemented in `src/lib/theme/markdown/admonitions.ts` via `remark-directive`; `:::unsourced` always renders in the reserved amber regardless of surrounding content, by design, so unsourced claims stay easy to spot before publication.

**Brand tokens:** `src/lib/theme/styles/tokens.css` is the single place all colors/type/spacing live, as CSS custom properties — components reference tokens, never raw hex values. **Synced to the real Brand Guide palette**, with one documented exception still marked PLACEHOLDER in the file: `--cw-danger` (the guide doesn't define one; decoupled from `--cw-freeway` since Freeway's real color is a neutral gray, not alarming). `--cw-intersections` is no longer a placeholder: as of 2026.08.30 it's a full site-only 6-tier ramp (`--cw-intersections-undeveloped` through `--cw-intersections-core`), built from a single base color (`#8a5a13`, the exact hex the old placeholder already used, so nothing that referenced the token changed color) with the same constant-hue/constant-saturation, evenly-stepped-lightness method the guide's own four ramps use. **Flagged, not fixed:** that hue sits within about a degree of `--cw-amber`, the color the guide reserves exclusively for the unsourced-claim flag and pattern-illustration highlighting — Intersections & Crossings now reads visually close to "amber warning" everywhere its color appears (PatternCard's left edge, PatternIndexCard's hover border and image tint). A deliberate user choice (they supplied the exact hex), documented on `/about/brand-guide/`'s Functional Classification ramps section rather than silently avoided. Key distinctions, easy to get wrong:

- `--cw-teal` is the raw brand hue (`#00A896`), for self-contained solid fills (buttons, badges, chips) where contrast against the page background doesn't apply. `--cw-primary` is a darkened/lightened variant of the _same_ hue, for text or thin accents sitting directly on the page background (hover states, active nav, focus ring, note admonition) — raw teal only reaches 2.86:1 on paper, under the 4.5:1 text minimum.
- `--cw-ink-fixed` / `--cw-paper-fixed` are non-theme-varying (defined once, never overridden in `[data-theme='dark']`), for text on a fill that's itself fixed (primary/dark buttons, the unsourced-claim flag, evidence chips). Using `var(--cw-ink)` there instead would flip to near-white in dark mode and go invisible against its own fixed-dark or fixed-colored background — this bit Button.svelte's "dark" variant during the sync, worth remembering.
- `--cw-link` / `--cw-link-hover` are the Brand Guide's indigo, used only by `base.css`'s global `a` rule for content hyperlinks, distinct from `--cw-primary`/`--cw-teal`.
- `--cw-amber` is reserved exclusively for the unsourced-claim flag and pattern highlighting per the guide, **never a general UI accent** — the global `:focus-visible` ring and the navbar active-link underline were both moved off amber onto `--cw-primary` for this reason (amber also only reached 1.97:1 against light paper as a focus ring, failing the 3:1 UI-component minimum). The `:::warning` admonition still uses amber and is a known, deliberately unresolved conflict with this rule, flagged in the Brand Guide page rather than silently changed.
- `--cw-local/-collector/-arterial/-freeway` each have a full 6-tier ramp (`--cw-local-undeveloped` through `--cw-local-core`, etc.) plus a single representative token (`--cw-local`, etc.) used by PatternCard's border accent, mapped to whichever tier clears (or gets closest to) 4.5:1 contrast — this differs by classification and by theme, see the comments in `tokens.css`. `-soft` variants reuse each classification's own lightest (Undeveloped) tier in light mode; dark mode needs hand-picked dark tints instead, since a near-white pastel looks wrong as a "soft" background on a dark page.
- `PatternCard.svelte`'s Pattern ID text is always `var(--cw-ink)`, never the classification color, per the guide ("a constant identity mark distinct from classification color") — this also happens to be load-bearing: Arterial and Freeway's real ramp colors don't clear 4.5:1 as text on either paper tone.
  The `[data-theme='dark']` block at the bottom is the dark-mode override for every token above (except the `-fixed` ones). `src/lib/theme/styles/base.css` builds base element and content typography from these tokens.

**CS-specific domain components** (`src/lib/theme/components/`, exported from `index.ts`):

- `PatternCard.svelte` — Pattern ID card; `classification` prop accepts `local | collector | arterial | freeway | intersections` and drives the left-border accent color (not the ID text, see above). Optional `status` prop (`recommended | situational | avoid`) renders a `StatusBadge` (see below) next to the classification label. Omit `status` entirely for Intersections & Crossings entries — the master Typology reference has no Status column for those, only for Local/Collector/Arterial/Freeway.
- `StatusBadge.svelte` — the `recommended | situational | avoid` pill itself, extracted out of `PatternCard.svelte` (2026.08.30) so `PatternIndexCard` could reuse the exact same badge instead of duplicating its CSS: quiet/neutral for `recommended` (no color emphasis, by design), amber for `situational` (reuses the `:::warning` token), rose for `avoid` (reuses `:::danger`).
- `PatternIndexCard.svelte` — the Pattern Index's card-grid entry (see the Pattern ID scheme note above for the full mechanism). `href`/`id`/`title`/`classification` are required; `status` is `situational | avoid` only (omit for Recommended, which gets no badge at all here, unlike `PatternCard`'s own quiet "Recommended" pill) and never applies to Intersections & Crossings; `image` is optional, omit to use the shared placeholder. The number badge is derived from `id`'s own trailing digits (`$derived`), not a separate prop, so it can't drift out of sync with the Pattern ID.
- `DensityChip.svelte` — Undeveloped → Core density-tier chip. Uses its own independent neutral-tan ramp, not the classification ramps, a deliberate site-only choice per its own comment; note this diverges from the guide's actual design (density tiers live _within_ each classification's own ramp there, not as a separate overlay) — a known, unresolved gap if this component gets revisited.
- `Citation.svelte` — numbered citation badge, and the unsourced-claim flag. Renders at 12px/semibold with a white text-shadow halo, not the guide's literal `--type-micro` (10px) — a deliberate legibility correction, documented on the Brand Guide page's Accessibility notes. Every badge links down to its source entry: `<Citation index={1} />` defaults its `href` to `#ref-1`, so any page's `## References` list must give each entry a matching anchor — write it as `1. <span id="ref-1"></span>Source text...`, a raw inline `<span>` is safe here (unlike a raw `<a>` tag, see the external-links note below) since it's a real HTML5 tag name that survives `allowDangerousHtml`'s opaque passthrough unmangled. `base.css` gives `[id^='ref-']` the same `scroll-margin-top` as headings so the jump clears the sticky navbar. Override `href` explicitly if a page's reference numbering doesn't follow the `ref-{index}` pattern, or (like Brand Guide's illustrative `<Citation index={14} href="#" />` demo, which has no real reference list behind it) the target genuinely doesn't exist: `adapter-static`'s `strict: true` prerendering fails the whole build on any same-page `#id` link with no matching element, so an un-overridden default `href` there breaks `pnpm run build`, not just a soft dead link. `href="#"` (no fragment id) sidesteps that check since there's nothing to validate.
- `EvidenceChip.svelte` — Legal / Evidence-based / Precedent-based tier chip (`tier` prop: `legal | evidence | precedent`). Standing house rule as of the sitewide evidence-tier rollout: every citation added to any page's `## References` list, from now on, must carry an `<EvidenceChip tier="..." />` right in that list item at the same time the citation is added — Legal/regulatory (a statute, adopted code, or government manual), Evidence-based (a peer-reviewed study, published crash data, a formal research report), Precedent-based (a real place doing it successfully, no formal study behind it). First adopted on Density Tiers and Street Types & Classifications' `## References` lists. Bicycle Boulevard's citations are tiered too, as of its 2026.09.02 full rewrite (see above) — its old 3 placeholder citations are gone, replaced by 2 real, tiered ones. Industry/association design guides that aren't government bodies (NACTO, CROW) don't cleanly fit any of the three tier definitions as written — treated as Legal/regulatory on the pages above as a judgment call, not a strict reading of the definition; revisit if this comes up again. **Placement and sizing convention:** the chip goes first, immediately after the `<span id="ref-N">` anchor and before the citation text (`N. <span id="ref-N"></span><EvidenceChip tier="..." /> Source text...`), not appended at the end — an end-of-line placement was tried first and reverted after review, since a reader has to reach the end of a long citation before learning its tier. The component's own CSS also renders it deliberately small (11px, tight padding) rather than the Brand Guide showcase's larger default, so it reads as a quiet tag rather than competing with the citation text for attention.
- `ColorSwatch.svelte`, `LogoPreview.svelte`, `ExampleCard.svelte`, `Button.svelte`, `FormControlsPreview.svelte` — Brand Guide page previews (`/about/brand-guide/`). All now token-driven, no hardcoded hex.
- `Hero.svelte`, `EvidenceStrip.svelte`, `FeatureGrid.svelte` — the homepage build (`src/routes/+page.md`). `Hero` renders its own `<h1>` rather than relying on `PageLayout`'s usual `fm.title`-driven one, so the title can sit inside the hero's own layout next to the tagline/description/actions; the homepage's frontmatter sets `hideTitle: true` to suppress `PageLayout`'s default `<h1>`, a flag `PageLayout.svelte` checks (`{#if fm.title && !fm.hideTitle}`) and only this page currently uses. The homepage frontmatter also sets `wide: true`, a second `PageLayout.svelte` opt-in flag (`class:cw-content--wide={fm.wide}`) that widens `.cw-content` from the standard 46rem prose column to 88rem, matching `Navbar.svelte`'s own `.navbar__inner` max-width, so `EvidenceStrip` and `FeatureGrid` (both inside `.cw-content`, unlike `Hero`'s own full-bleed section below) get a wider column than an article page without a third breakout mechanism. `EvidenceStrip` uses a teal/`--cw-primary-soft` card treatment (border-left accent, drop shadow), not amber: an earlier version used `--cw-amber-soft` (the `:::warning` admonition's own background), which read as a caution notice instead of a feature highlight and was also a genuine stretch of the Brand Guide's "amber reserved for pattern highlighting and the unsourced-claim flag" rule. Its "Learn how evidence tiers work" link is a plain content link with no color override, inheriting `base.css`'s global `a` styling like any other in-page link, after an earlier amber-matched version didn't read as a normal hyperlink. **A real specificity gotcha hit while building these:** `base.css`'s generic `.cw-content h1/h2/p/ul/li` prose rules (sized for markdown body text) beat a same-page single-class selector on specificity, so any of these three components' rules that override a bare element's margin/size/grid-columns needs a `:global(.some-ancestor-class)` prefix to win — and critically, once a *base* rule gets that prefix, every `@media` breakpoint rule touching the same property needs the identical prefix too, or the base rule's higher specificity wins outright regardless of which media query is active (this silently broke `FeatureGrid`'s mobile 1-column and 2-column breakpoints, and `Hero`'s mobile title font-size, during the initial build — both now fixed by prefixing the media-query rules to match).

  **`Hero`'s section is full-bleed and permanently dark**, deliberately not following the site's own light/dark toggle: `GlobalLayout.svelte` computes `isHome = page.url.pathname === '/'` and, only on that route, collapses `.shell`'s 3-column grid (`sidebar content toc`) down to a single `content` column via a `.shell--full` class (declared last in that file's `<style>` block specifically so it wins the specificity tie against the two responsive `.shell` breakpoint rules above it, at every viewport width, not just where it's declared) and hides `Sidebar`'s rendered `<aside>` at `>=940px` via `.shell--full :global(.sidebar) { display: none }`. `Sidebar` itself stays mounted unconditionally even on the homepage: below 940px it's still the only way to reach the navbar's primary links from the mobile hamburger drawer (see the `sidebar__primary-nav` note above), and `position: fixed` there already takes it out of grid flow, so it doesn't need hiding at that width. `Hero.svelte`'s own `.hero-bleed` then escapes `PageLayout`'s `.cw-content` 46rem max-width (and the padded `.page` around it) with the standard `left:50%; margin-left:-50vw; width:100vw` breakout trick, which works through however many centered ancestors sit in between since `100vw` is always the true viewport width; this only avoids introducing a horizontal scrollbar because `base.css` already sets `scrollbar-gutter: stable` on `html`. Because dark-mode's own page background (`--cw-paper` = `#141414`) is identical to `--cw-ink-fixed`, `Hero`'s `--hero-bg` custom property gets a `[data-theme='dark']` override to a deep teal-charcoal (`#0e211d`) so the hero still reads as a visually distinct band in dark mode too, not just light mode where the plain fixed-ink background already contrasts against the light paper. Its diagonal-dash background (a deliberate nod to lane markings, this being a roads-and-streets guide) and the radial teal glow behind the logomark both animate only under `@media (prefers-reduced-motion: no-preference)`, left static otherwise. Any future full-bleed section on another page should reuse this same `isHome`-gated pattern rather than inventing a second one; it currently only exists for the homepage.

Layout/UI components in the same directory (`GlobalLayout`, `PageLayout`, `Navbar`, `Sidebar`/`SidebarGroup`, `Toc`, `PageNav`, `Icon`, `ThemeToggle`, `Backdrop`, `ShareButtons`) implement the shell; `src/lib/theme/layout.ts` holds the Svelte stores for sidebar open/closed and dark mode state.

**`Navbar.svelte` below 940px (mobile-fixed 2026.08.30):** the primary nav links move into `Sidebar`'s hamburger drawer at this breakpoint, which used to leave only 2 flex groups (`navbar__start`: logo+hamburger, `navbar__end`: version+GitHub+toggle) sitting adjacent with a fixed gap, clumped on the left with empty space on the right, since nothing filled the middle once `navbar__links` (`flex: 1 1 auto`) disappeared. Fixed by unwrapping both groups into flat children of `navbar__inner` via `display: contents` inside the same `@media (max-width: 940px)` block, letting `justify-content: space-between` (added unconditionally to `.navbar__inner`, harmless at desktop widths since `navbar__links`' own flex-grow already consumes all remaining space there) spread all 5 elements evenly. Two things that looked like they'd be enough but weren't, confirmed by real measurement, not eyeballing: shrinking the version text alone still overflowed at 375px, since the full wordmark logo (147px, over a third of the viewport) was the bigger space hog, not the text; and `navbar__inner`'s existing `gap: 1.5rem` (sized for the old 2-group layout) applied 4× over across the 5 unwrapped children, ~96px of unwanted spacing, so the mobile breakpoint also sets a smaller `gap: 0.5rem` and pins `flex-shrink: 0` on all 5 items (menu button, brand, version, icon-link, toggle) — without it the icons were silently compressing below their own 36px width under crowding pressure, a real tap-target regression the fix's own test criteria were meant to catch.

**Known bug, not yet fixed (found 2026.08.30):** the mobile hamburger button doesn't actually toggle the sidebar drawer open on any viewport — `sidebarOpen`'s store state does flip (confirmed via the button's own `aria-label` changing between "Open/Close navigation"), and `Sidebar.svelte`'s `.sidebar--open { transform: translateX(0) }` CSS rule is present and higher in source order than the base `.sidebar` rule, but the drawer's computed `transform` never actually changes, so it stays off-screen. Confirmed on an untouched copy of `main` in an isolated `git worktree`, so this predates any recent session's changes and isn't caused by the mobile navbar layout fix above. Net effect: on any viewport narrower than 940px, the primary nav links and the current page's sidebar tree are currently unreachable through the hamburger button. Root cause not yet diagnosed.

- `ShareButtons.svelte` — the "Share:" row in `PageLayout.svelte`'s footer (X, Facebook, LinkedIn, Reddit, Bluesky, email, copy-link), shown on every page uniformly. Every network is a plain `<a>` to a public share-intent URL (`x.com/intent/tweet`, not `twitter.com`, confirmed live — `twitter.com` still works but just redirects), **no share SDK/widget script** (Twitter's `widgets.js`, Facebook's SDK, AddThis, ShareThis) — those set their own third-party cookies, which would contradict `/about/privacy-policy/`'s "no other tracking beyond what's listed" claim. Copy-link is the one bit of real client JS (Clipboard API), fails silently if unavailable. Its root element is a `<span>`, not a `<div>`: it mounts inside `PageLayout.svelte`'s `<p class="page__meta">`, and a block-level root there is invalid HTML that browsers silently "fix" by force-closing the `<p>` early — easy mistake to reintroduce if this component gets restructured. The visible "Share:" label sits in that same paragraph's own inline text flow rather than inside the icon row's `inline-flex` container, since centering it against the icons' taller box height (not the surrounding text's baseline) is what caused a real 1-2px vertical misalignment, only visible at the one width wide enough to fit "Suggest changes…", "Last updated…", and the share row on one line.
- `Icon.svelte`'s `filled` prop (default `false`, preserves every original icon's stroke-only rendering): the share-button brand marks (`facebook`, `linkedin`, `reddit`, `bluesky`) render `fill="currentColor" stroke="none"` instead, since a solid logomark only reads correctly filled, not traced as a line icon (that's also why `github`, the one pre-existing "logo" icon, looks slightly off as a stroke — established behavior, not touched). The shared `<path>` also always carries `fill-rule="evenodd"`, needed only so the `reddit` mark's eye circles punch through as holes rather than double-filling invisibly on top of the head circle (a no-op for every stroked icon and every filled icon without overlapping subpaths). `reddit` and `bluesky` are simplified original glyphs, not pixel-exact traces of the real logos — verify any new icon by rendering it standalone (not just trusting the SVG path data by eye) before shipping.

**Adapter:** `@sveltejs/adapter-static`, `strict: true`, output to `build/` — the whole guidebook prerenders to static HTML with every pattern page a real crawlable URL. `src/routes/+layout.ts` turns on prerendering and trailing-slash URLs; `src/routes/+layout.svelte` is deliberately minimal since SveltePress wraps it in `GlobalLayout`.

**Retiring or redirecting a page (first used 2026.09.02, the Commercial Arterial → Stroad merge):** a real HTTP redirect isn't possible from inside the app on a fully static, server-less build — SvelteKit's own `redirect()` only works from a server `load` function, which `adapter-static` doesn't run at request time. The fix is `static/_redirects`, Netlify's own static-redirects file format (`/old-path/ /new-path/ 301`, one rule per line): Netlify reads it directly at the CDN edge, no server or build-time plugin involved, and it gets copied into `build/` like any other static asset. Delete the retired route folder entirely rather than leaving a stub page (a stub would itself need `strict: true`-safe content and just adds a page that immediately fights the redirect). This can't be verified in `pnpm run dev`/`pnpm run preview` — neither reads `_redirects` — only confirm the redirect rule is present in `build/_redirects` after `pnpm run build`; the actual redirect only takes effect once deployed to Netlify.

**"Last updated" date** (`PageLayout.svelte`'s footer, next to "Suggest changes to this page"): sourced from `src/routes/+layout.server.ts`, which runs `git log -1 --format=%cI -- <that route's +page.md>` once per route at build time and formats the result, `null` (no line rendered) if the file has no commit yet or git isn't available. Deliberately **not** the file's filesystem mtime — a fresh CI checkout (Netlify's included) resets every file's mtime to the checkout moment, not its real last-edit date, so mtime would just always show the build date. This is a real server `load` function (needs `@types/node` for `child_process`/`process`), but it still fully prerenders under `adapter-static`: it runs once per route during the build and the result is baked into the static output, no runtime server involved, same "computed at build time" reasoning `siteVersion()` (below) also uses.

**Navbar CalVer version (fixed 2026.09.01):** `Navbar.svelte` no longer computes its own version — it reads `page.data.siteVersion`, set by `siteVersion()` in the same root `src/routes/+layout.server.ts` described above (`src/lib/site.ts`'s `calVerToday()` is gone; `formatCalVer(year, month, day)` is its pure-formatting replacement, called from both branches below). The original design (a plain `new Date()` read wherever `calVerToday()` was called) worked in `pnpm run dev` but was wrong in production: a prerendered build's wall clock reflects whenever Netlify happened to actually run that build, not the date of the commit being built, and those two dates silently diverge any time Netlify rebuilds the same commit later without a fresh push (a manual "Trigger deploy" / "Clear cache and deploy site" click, or an automatic retry) — confirmed live on 2026.09.01, when the navbar showed a date one day ahead of every page's own git-derived "Last updated" text after exactly that kind of same-day rebuild. `siteVersion()` fixes this by keying the version to the actual last commit's date (`git log -1 --format=%cI`, no path filter, i.e. `HEAD`) instead of the wall clock, gated on SvelteKit's `dev` flag from `$app/environment`: `pnpm run dev` still shows a live current-date read (there's no meaningful "last commit" to key off mid-edit), but `pnpm run build`/`pnpm run preview`/the real Netlify build all show the last commit's own date, which only changes when new content is actually pushed, and stays identical no matter how many times that same commit gets rebuilt. If this ever needs re-diagnosing: compare the navbar version against any page's own "Last updated" text — they're computed by the same `git log` mechanism now and should never disagree the way they did before this fix.

**`llms.txt` / `robots.txt` / `sitemap.xml`:** `src/routes/{llms.txt,robots.txt,sitemap.xml}/+server.ts` are `+server.ts` endpoints, not files under `static/`. `src/lib/server/content.ts` globs every `+page.md` at build time (`import.meta.glob(..., { query: '?raw' })`, parsed with the `yaml` package) and reads each page's `llms` frontmatter field (falls back to `description`, then `title`) to build `llms.txt`; `sitemap.xml` uses the same `getSitePages()` helper to list every page's absolute URL with a build-date `<lastmod>`. New pages appear in both automatically. Because prerendering is on, all three compile down to plain static files in `build/` at build time, same as every page, no server needed at runtime, safe for a Netlify/adapter-static deploy. Nothing links to any of them, so they're listed explicitly in `svelte.config.js`'s `kit.prerender.entries` or the crawler skips them. `robots.txt` points at `sitemap.xml` via a `Sitemap:` line. `src/lib/site.ts` holds the canonical `SITE_URL` used for all three routes' absolute links, plus the `isExternalHref`/`externalLinkAttrs` helpers (see external-links note above).

**Google Analytics:** `src/lib/theme/components/GoogleAnalytics.svelte`, rendered once from `GlobalLayout.svelte`, injects the gtag snippet via `<svelte:head>` when `options.googleAnalyticsId` is set (currently `G-VCDS38HY87` in `vite.config.ts`) **and** `import.meta.env.PROD` is true, so `pnpm run dev` never sends traffic. SveltePress's own default theme has a built-in `ga` config option that auto-injects this, but that's a default-theme-only feature; `@sveltepress/theme-default` isn't installed here (this theme is fully custom, see top of file), so it had to be hand-built. **Do not write a literal `<script>...</script>` tag inside this component's own `<script>` block** (even inside a string or comment) — Svelte's compiler misparses the literal substring `<script` as a nested tag boundary regardless of quoting; the tag text is built from concatenated string pieces (`` `<${'script'}` ``) instead, so the literal substring never appears in the source.

**JSON-LD structured data (added 2026.08.30):** every page emits `<script type="application/ld+json">` blocks via `JsonLd.svelte`, built from pure functions in `src/lib/theme/schema/` rather than hand-authored per page, keyed to `schema-dts` types for compile-time-checked shape. Two injection points, split by what each layout actually has access to: `GlobalLayout.svelte` renders the sitewide singletons (one `Organization`, one `WebSite`, `organization.ts`, computed once, page-independent) since it wraps every route; `PageLayout.svelte` renders the per-page schema (`BreadcrumbList` from `breadcrumbs.ts`, plus a template-appropriate type from `page.ts`: `TechArticle` for Guide/Reference/pattern pages, `CollectionPage`/`ItemList` for the Pattern Index sourced from the same `sidebar` data that renders its own nav, `AboutPage`/`WebPage` for About, `Blog`/`BlogPosting` for the blog reusing the existing `getBlogPosts()` helper) since only it receives `fm`. `breadcrumbs.ts` deliberately skips sidebar group headers with no `to` of their own (e.g. "Local Streets," "Foundations") rather than linking to a nonexistent classification-landing-page URL — see the Known gaps entry on those pages below. Every page's frontmatter grew a `date` field (`datePublished`, backfilled from git history the same date the "Last updated" text already showed, since no earlier true publish date is tracked) and every pattern page grew a `patternId` field (e.g. `LOC-ALY-02`), which `PatternCard`'s `id` prop now reads via `{fm.patternId}` instead of a hardcoded literal in the component tag, one source of truth for both the visible card and the `identifier` field in that page's `TechArticle` schema. `+layout.server.ts` also now returns `lastModifiedISO` (a plain `YYYY-MM-DD`, not the full `%cI` offset string) alongside the existing human-formatted `lastModified`, same git call, feeding `dateModified` in the schema.

**Pattern ID scheme (corrected to nine prefixes, 2026.08.31):** `[PREFIX]-[CTXCODE]-[##]` with an optional trailing `(DENSITY)` suffix (`UND`/`RRL`/`SUB`/`CPT`/`URB`/`COR`), e.g. `LOC-BBG-12`. `PREFIX` encodes Scale: `NET` (Network), `CDR` (Corridor), `FAC` (Facility), and `ELM` (Element) are flat, non-subdividing prefixes, one per Scale; `LOC`/`COL`/`ART`/`FRE` (the four Functional Classifications) and `INT` (Intersections & Crossings, cross-cutting, its own top-level code rather than nesting under one classification) are the five prefixes at the Segment/Intersection scale, the only Scale that subdivides by Functional Classification. `CTXCODE` is a hand-designed three-letter mnemonic; each classification's baseline/default entry uses `DEF` (not the classification's own letters). Two different prefixes can legitimately share the same `CTXCODE` (`ELM-TRB-23` Traffic Barrier and `INT-TRB-02` Turbo-Roundabout), not a bug, same precedent as `COR`/`COL`. **The number is two digits, assigned once per Typology within its prefix in first-added order, and is never reassigned or reordered to match display/alphabetical order later** — this is the rule that broke twice during scaffolding (see `src/routes/patterns/+page.md` for the current full mapping, and the pattern-pages project memory for the format's version history if a stale mapping ever surfaces again). The `(DENSITY)` suffix is added only once a second page exists for the same Typology at a different density tier; omit it otherwise, don't pre-add it. **106 patterns are live as of the 2026.08.31 batch build-out**, every one of them with a real `+page.md`, no more descriptive-only placeholder tables: 4 Network (`src/routes/patterns/network/`, deliberately singular, unlike every other category folder below — matches the route convention given when this batch was scoped, not a typo to "fix" into `networks/`), 6 Corridor (`corridors/`), 13 Local + 7 Collector + 8 Arterial + 4 Freeway + 11 Intersections & Crossings (43 total Segment/Intersection, `local-streets/`, `collectors/`, `arterials/`, `freeways/`, `intersections/`), 10 Facility (`facilities/`), and 43 Element (`elements/`). This batch resolved every previously-open item: the three formerly "under review" Typologies now have real pages and Pattern IDs (Downtown Thoroughfare `COL-DTH-07`, Commercial Arterial `ART-CAR-07`, Parkway `ART-PKY-07`, renumbered from `ART-PKY-08` on 2026.09.02, see below), and the Pattern Index's old "On the Horizon" descriptive tables and "Under review" bullet list are both gone, their contents built out into real pages instead. Two names from the original "under review" wording were deliberately never built: Avenue and the Downtown One-Way/Two-Way Street cluster are not real patterns (Downtown One-Way is just an instance of the existing One-Way Street, `ART-OWS-06`; Avenue was dropped from consideration). Each pattern page's numeric suffix is now also duplicated into a `patternId` frontmatter field (see JSON-LD note above), so the two must be kept in sync by hand if the ID scheme ever changes.

**Pattern IDs page split out of How to Read a Pattern (2026.09.01):** the nine-prefix Scale table and the status-badge explanation used to live inline in How to Read a Pattern's "The Pattern ID card" section; both now live on their own page, `src/routes/guide/pattern-ids/` (sidebar: Foundations, right after "How to read a pattern"). How to Read a Pattern's own section is now short: a live example card, then a paragraph or two of orientation, then a link out to Pattern IDs for the full breakdown. The live example reuses the Brand Guide's exact illustrative card (`<ExampleCard><PatternCard id="ART-UMS-09" ...></ExampleCard>`, same fabricated `ART-UMS-09` "Urban main street" data, imported from `$lib/theme/components`) rather than a new component or a real pattern's data, so the sample stays decoupled from any real pattern's content changing later — reuse this same `ExampleCard`-wrapped `PatternCard` pattern for any future "show a live sample inline" need instead of building a bespoke mockup. **When content that other pages link to by topic (not just by URL) gets split off a page like this, grep for pages describing that specific content, not just pages linking to the old URL** — `street-types/+page.md` said "See How to Read a Pattern for the full nine-prefix ID scheme," which needed to repoint at Pattern IDs even though the How to Read a Pattern link itself still resolves fine; a plain link-target grep alone would have missed that this specific claim about page content was now wrong.

**Pattern Index page (`/patterns/`) is a card grid, not a bulleted list (rebuilt 2026.08.30, restructured to 9 Scale sections 2026.08.31):** nine `## `-level sections in Scale order (Network, Corridor, Local street, Collector street, Arterial street, Freeway, Intersections and crossings, Facility level, Element level), each a `<div class="pattern-index-grid">` of `<PatternIndexCard>` tags (one per pattern, hand-authored in the markdown, not generated from a data file), `.pattern-index-grid`'s CSS living in `base.css` alongside `.swatch-grid`/`.demo-row` (same "Brand Guide-style layout utility" convention, not a component-scoped style) with the same 3/2/1-column breakpoints as `FeatureGrid.svelte`. The old "Under review, no confirmed Functional Classification" bulleted list and "On the Horizon" descriptive tables are both gone as of the 2026.08.31 batch: every pattern that was named-but-not-built now has a real page and a card. `PatternIndexCard.svelte`'s image area uses one shared placeholder graphic (`static/patterns/pattern-placeholder.svg`) by default, recolored per classification with a `mix-blend-mode: color` overlay (Suburban tier at rest, Core tier as the hover border) plus a flat `filter: brightness(90%)` on the image itself first, since the real illustrations being added are mostly-white line art and white has nowhere to go under a color blend otherwise. A pattern gets a real illustration by adding a PNG to `static/patterns/` (naming convention: `{pattern-id-lowercased}-{slug}.png`, e.g. `loc-aly-02-alley.png`) and passing it as that card's `image` prop in `patterns/+page.md`; omitting `image` falls back to the placeholder, no other change needed. As of 2026.08.30, 11 of 106 patterns have real illustrations (all of Local Streets except Bicycle Boulevard, plus Roundabout), the rest still show the placeholder pending more art. `StatusBadge.svelte` was extracted out of `PatternCard.svelte` during this build so both it and `PatternIndexCard` share one status-pill implementation instead of duplicating the CSS.

**PLACEHOLDER, needs a follow-up decision:** `PatternCard.svelte` and `PatternIndexCard.svelte`'s `classification` prop grew four new values on 2026.08.31 — `network`, `corridor`, `facility`, `element` — for the batch above. No brand color is defined for any of the four; all four currently reuse `--cw-freeway`/`--cw-freeway-suburban`/`--cw-freeway-core` (Freeway's own ramp, already a neutral gray) as a stand-in border/overlay color, flagged inline in both components' CSS. Swap for real per-Scale colors once assigned, the same way Intersections & Crossings got its own real ramp after starting on a placeholder.

**Citations (completed 2026.09.01):** all 106 pattern pages, `local-streets/bicycle-boulevard/` included as of its 2026.09.02 full rewrite (see below), now carry a real `## References` section (numbered list, each entry `N. <span id="ref-N"></span><EvidenceChip tier="..." /> citation text`) plus inline `<Citation index={N} />` badges in the prose pointing down to those entries — see `Citation.svelte`'s note above for the anchor mechanics. This was done in two batches: 46 pages already had References from an earlier session but no inline badges (badges added, References untouched), and 47 pages got both References and badges built from scratch (mostly Corridor, Local Street, Collector, Arterial, Freeway, Intersection, and the two remaining Traffic Calming patterns). MUTCD citations site-wide read "MUTCD 11th Edition with Revision 1 (FHWA, December 2025; effective March 5, 2026)" plus a section/chapter cite — a real edition/revision, not a placeholder, but section numbers weren't individually re-verified against the Revision 1 PDF, worth a spot-check before treating any MUTCD citation as authoritative. A few badge placements are "closest topical match" rather than exact hits where the source completion package described a specific bullet/sentence that didn't literally exist in that page's prose (e.g. RRFB's crash-stat bullets, Curb Extension's "when to use it" bullet) — not wrong, just worth a read-through rather than assuming every citation lands on the literal sentence it's evidence for. Two pages intentionally have `<Citation>` inside the `<PatternCard>` component's own body text rather than a separate prose paragraph (Boulevard, Scenic Highway/Byway, Stroad, and all four Freeway pages lack any lead paragraph outside the card) — confirmed this renders and resolves fine under `adapter-static`'s strict prerendering, not a bug.

**Stroad / Commercial Arterial merge (2026.09.02):** Commercial Arterial (`ART-CAR-07`) and Stroad (`ART-STD-05`) documented the same failure mode under two different names — through traffic mixed with on-street parking, direct driveway access, or both. `ART-CAR-07` is retired, not reassigned, and its route (`/patterns/arterials/commercial-arterial/`) now 301-redirects to `/patterns/arterials/stroad/` via `static/_redirects` (Netlify's own static-redirects file — the way to get a real HTTP redirect on `adapter-static` with no server, since SvelteKit's own `redirect()` only works from a server `load`). Stroad's page absorbed Commercial Arterial's citations (now 3 references) and gained a `## Related patterns` section; its display title is now "Stroad (Commercial Arterial)" though the URL slug and Pattern ID are unchanged. Main Street's stale "under-review Avenue/Commercial Arterial cluster" cross-reference was corrected to point at Stroad by name.

**Intersections & Crossings status badges (2026.09.02):** reverses the prior documented convention (previously: intersections carry no status, since "the master Typology reference only tracks Recommended/Situational/Avoid for Local, Collector, Arterial, and Freeway rows, not intersections"). All 11 Intersections & Crossings pages now carry a status (`recommended` or `situational`; none are `avoid`), applied to both each page's own `PatternCard` and its `PatternIndexCard` row on the Pattern Index. `PatternCard`/`PatternIndexCard`'s own doc comments were updated to match — check those, not this note, if this ever needs re-confirming.

**Bicycle Boulevard rewrite, template-consistency pass, and the Illustration component system (2026.09.02, Tickets D/E/F):** Bicycle Boulevard / Neighborhood Greenway (`LOC-BBG-12`) was replaced end to end, dropping its "demonstrates the components, not final text" placeholder framing for real content and 2 real references. Four pages (Parkway, Downtown Thoroughfare, Multi-Use Trail / Greenway, Shared-Use Path) were converted off a non-standard "Why this pattern exists" / "Key considerations" template onto the sitewide "Modal hierarchy" → "Design notes" → "Related patterns" (where present) → "References" structure; existing citation numbers and their attachment points were preserved, only the surrounding prose and headings changed.

A new `Illustration` component system landed for the Brand Guide's illustration spec: `Illustration` (wrapper, `type`/`format` props), `IllustrationFrame` (`label`/`tone` props, `tone="grayscale"` or `"amber"`), `IllustrationCallouts` (numbered-list wrapper), `IllustrationCallout` (`number`/`kind` props — `kind` is `physical | connectivity | control-timing`; `number` is explicit rather than relying on `<ol>` auto-increment, using the HTML `<li value>` attribute, since one illustration's callouts can split non-contiguously across a required + a supplemental box, see Stroad below), and `IllustrationInsteadSee` (the required closing field for Single-condition/Avoid-pattern illustrations). Applied so far to Local Default (`LOC-DEF-01`, replacing its old "Cross-section" NOTE placeholder, which also meant adding a new reference, NACTO's Lane Width guidance) and Stroad (`ART-STD-05`, a required Single-condition illustration plus a justified plan-view supplemental, reusing its existing NCHRP 420 reference). This is a sitewide component now available for every future pattern page's Illustration section, but illustration *content* has only been authored for these three pages (Bicycle Boulevard, Local Default, Stroad) — the other ~103 pattern pages still have no Illustration section, that content doesn't exist yet and needs drafting per pattern before it can be added.

**Related Patterns rollout completed, guide updated, post-verification cleanup (2026.09.02, Tickets G-J):** Protected Bike Lane / Cycle Track (`ELM-PBL-37`) gained a 4th Key Considerations bullet citing its existing Reference 2 (NACTO minimum-width and buffer-width specs), which had been listed but never cited inline. The 35 pattern pages still missing a `## Related patterns` section (11 Local, 5 Collector, 5 Arterial, 4 Freeway, 10 Intersections) all got one, placed immediately before `## References` — every one of the 105 live pattern pages now has exactly one. How to Read a Pattern gained two new sections matching this: "Illustrations" (reader-facing only, no color/amber-percentage internals) between "The Pattern ID card" and "Density tiers", and "Related patterns" after "Citations"; two stale cross-references were also fixed (Bicycle Boulevard's citation-example pointer, which named a "Design Speed" section that no longer exists post-rewrite, corrected to "Design notes"; and the `:::warning`/"Caution" admonition example, which had cited a Bicycle Boulevard warning block that no longer exists post-rewrite, replaced with a real, grep-verified `:::warning` example from One-Way Street).

A follow-up verification pass (Ticket J) then caught three more issues, all tracing back to the Stroad merge and Bicycle Boulevard rewrite above: a leaked instruction sentence ("Citation badge 1 also appears once more, in Illustration callout 3 above.") had rendered as literal visible page text on Bicycle Boulevard, immediately after the Design notes paragraphs — it was meta-commentary from the ticket file explaining *where else* a citation appears, never meant to be page content, and got included when that section was drafted. Deleted; a sitewide grep for similar leaked meta-commentary (`Ticket [A-Z]`, "Claude Code", "also appears... above", etc.) turned up nothing else. **Watch for this failure mode specifically after any ticket-driven content rewrite: proofread the rendered section for sentences that read like instructions-to-Claude rather than content-for-readers, not just for factual accuracy.** Separately, the Pattern Index's Arterial table had a visible numbering gap (1,2,3,4,5,6,**8**, no 7) left over from the Commercial Arterial retirement, since `PatternIndexCard`'s number badge is derived directly from the Pattern ID's own trailing digits (see `PatternIndexCard.svelte`), not a separate listing-position prop — closing it meant either renumbering Parkway's ID or adding a decoupled prop. The user explicitly authorized breaking the "IDs are never reassigned" rule as a one-time exception given the site's four-day-old, not-yet-public status; Parkway is now `ART-PKY-07` (was `ART-PKY-08`) everywhere except the 2026.08.31 changelog entry documenting its original creation, which was deliberately left saying `ART-PKY-08` since that's the historical record of what the ID was at the time — **this is the house rule going forward: a later ID renumbering updates every live cross-reference except the changelog entry that originally announced the old ID, which stays as written.** See the Pattern ID scheme note above for the full renumbering writeup.

**Confirmed: `pnpm run dev` and `pnpm run preview` cannot exercise `static/_redirects` at all**, not even partially — both just 404 on a retired route's old URL, since neither is Netlify's edge and neither parses that file format. The only pre-deploy checks available are (1) the rule's presence in `static/_redirects` and (2) that it survives into `build/_redirects` byte-for-byte after `pnpm run build`; the redirect itself only becomes observable once pushed and rebuilt on Netlify (or via Netlify's own CLI, `netlify dev`, if that's ever installed here — not currently the case).

**Real gotcha hit building `IllustrationInsteadSee`:** markdown link syntax (`[text](url)`) does not resolve when it sits inside a component that is itself nested inside another component's children — confirmed live, it rendered as literal bracket text (`[Boulevard](/patterns/...)`) instead of a link, even after ruling out indentation/code-block misinterpretation as the cause. No page anywhere on this site nests a markdown link inside a component slot (every existing markdown link sits in top-level page prose); `IllustrationInsteadSee` now takes a structured `links` prop (`{ label, href, id, joiner? }[]`) and renders real `<a>` tags itself instead, the same "hand-render an internal link from the component" pattern already used by `PageLayout`'s edit link and `Navbar`'s nav links. If a future component needs to show a link inside a doubly-nested slot, use this same structured-prop pattern rather than markdown link syntax.

**Two more precise root causes found for this same general class of bug, while building the Speed & Modal Hierarchy card (2026.09.02) — both now-understood special cases of "content inside a nested component slot isn't parsed as markdown the normal way," not two separate bugs:** (1) Indenting a markdown-embedded Svelte component's content by 4+ spaces for source readability, when that content starts right after a blank line, gets parsed as a CommonMark indented code block, silently breaking any markdown inside it (an ordered list rendered as literal `1. Pedestrian` text, and swallowed a same-line raw `<a>` tag into the code block where it rendered as escaped text instead of a link). Fix: never indent multi-line component content, even though every sibling tag around it is indented — write it flush left. (2) A markdown-embedded component tag carrying a complex prop value (an object or array literal, e.g. `rows={[{ tiers: [...] }]}`) fails CommonMark's own HTML-block-recognition regex and gets treated as plain paragraph text instead of a component, **specifically when a blank line immediately precedes it** — confirmed by the exact same tag succeeding once the blank line above it was removed (making it a continuation of the prior tag's line instead of a fresh block) and failing again when the blank line was restored. A tag with only simple string/number attributes doesn't hit this. Fix: never put a blank line directly before a component tag that has an object/array-literal prop; self-closing vs. open+close tag form didn't matter once this was fixed. Both fixes are now baked into every page using `SpeedModalHierarchyCard`/`SpeedLimitSection`/`ModalHierarchySection` — copy their markdown structure (flush-left, no blank line before a complex-prop tag) rather than the more readable indented style used elsewhere on the site, if adding a similarly nested component in the future.

## The Speed & Modal Hierarchy card, Design Speed & Speed Limits guide page (2026.09.02)

**New Guide page, `guide/design-speed/`** (Foundations, after Scale Hierarchy): explains why design speed, 85th-percentile operating speed, and posted speed limit diverge (the 1985 FHWA memo vs. the 2001 AASHTO Green Book redefinition), then states CS's own rule, design speed always equals posted speed, with the concrete numbers by Character Type (Local 20 mph flat; Collector 30, 35 only with rigid separation; Arterial a density-tiered baseline with two non-stacking −5 mph modifiers and a 30 mph floor; Freeway deferred to existing regulatory systems). Built from a user-supplied ticket that used raw `<sup class="citation">` HTML for citations; translated silently to this site's real `<Citation index={N} />` / `<EvidenceChip tier="..." />` convention per the standing "verify a ticket's claims against the actual code" rule below, since that's an authoring-syntax gap, not a content decision.

**New reusable component system, `SpeedModalHierarchyCard`** (`src/lib/theme/components/`): replaces the old plain "Modal hierarchy" heading/list on every automobile-allowing Local, Collector, and Arterial pattern page (23 of them, all three Defaults included), and extends to 4 patterns that exclude automobiles entirely (Pedestrian-only Street/Plaza, Shared-Use Path, Bike Highway/Greenway, Multi-Use Trail/Greenway) in a Modal-Hierarchy-only form. Composed the same nested-nested-component way `Illustration`/`IllustrationFrame` already established:

- `SpeedModalHierarchyCard` — outer shell, just lays out whatever children it's given.
- `SpeedLimitSection` (Section A) — props `speeds: number[]` (deduped/sorted ascending, rendered as one `SpeedLimitSign` per value), `guideLink?: boolean` (default `true`; `false` on the three Default pages, whose own prose already carries the full sourced rule — every other page gets a real, component-rendered "See Design Speed & Speed Limits" link, not a markdown one, to sidestep the nested-link bug above), and `stacked?: boolean` (default `false`; `true` on every Arterial page only, since Arterial is the one Character Type that can carry up to 5 signs and the side-by-side layout got cramped — Local/Collector never set it). **Real bug found and fixed:** `stacked` naively reused the same `flex: 1 1 260px` on the description text as the side-by-side layout; in `flex-direction: column`, that flex-basis applies to the vertical axis, forcing a 260px-tall box regardless of actual content height and leaving a visible gap before the divider (most visible on 3-sign pages like Boulevard). Fixed with a `[data-stacked='true']` override to `flex: 0 1 auto`.
- `ModalHierarchySection` (Section B) — props `classification?: 'local'|'collector'|'arterial'` and `rows?: { label?: string, tiers: (Mode|Mode[])[] }[]`, rendering one `ModalHierarchyRow` per row internally (never author `<ModalHierarchyRow>` directly in markdown — it used to be, until the complex-prop-plus-blank-line bug above was diagnosed there first). Omit `classification`/`rows` entirely for a plain-prose Section B (Stroad: an Avoid pattern with no ranked hierarchy, "no default modal order is assigned" stays plain text, no pill row). Pass 2 `rows` with a `label` each for the two time-conditional patterns (Festival Street: "During events:" / "Off-hours:"; School Street: "During active hours:" / "Off-hours:"). A card whose only child is a `ModalHierarchySection` (the 4 non-automobile patterns, no `SpeedLimitSection` sibling) gets its divider/margin/border-top stripped via a `:first-child` selector in `SpeedModalHierarchyCard`'s own CSS, so there's nothing to divide from.
- `ModalHierarchyRow` — renders one ranked row of `ModalHierarchyPill`s, `>` between tiers, `=` within a tied tier (`tiers` accepts a mode string or an array of tied mode strings per rank — a real, live capability, not speculative: Shared-Use Path and Multi-Use Trail/Greenway's own "Pedestrian and Bicycle share top priority, no ranking between them" content is a genuine tie, `tiers={[['pedestrian','bicycle']]}`).
- `ModalHierarchyPill` — icon + mode name, one of 5 modes (pedestrian/bicycle/transit/vehicle/freight). Icons are 5 exact Lucide icons (person-standing, bike, tram-front, car, truck) the user supplied directly as SVG markup, ISC-licensed; see `THIRD-PARTY-NOTICES` at the repo root (linked from `/about/terms-and-conditions/`'s "Code and content licensing" section) — none of the 5 fall under Lucide's Feather-derived MIT subset, so only the ISC notice applies. Sized to match `DensityChip`. Each icon sits in a `rgba(0,0,0,0.2)` circle badge. Background is the pattern's own classification color (`--cw-local`/`-collector`/`-arterial`, the *same* token `PatternCard`'s left border already uses — **not** a fixed "Core tier," despite an early ticket claiming that; it's whichever tier that token already points to, which differs by classification and by theme, see `tokens.css`). Text/icon color is a dedicated `--cw-*-pill-fg` token (`#fff/#000`, computed via real WCAG math, not eyeballed) chosen per classification per theme for the larger contrast margin — except Arterial, a **deliberate, user-directed exception**: black wins the math in both themes (5.88:1 light, 9.06:1 dark) but reads far less legibly against Arterial's saturated yellow-green than white does in practice, so Arterial is fixed white in both themes (3.57:1 light, 2.32:1 dark — both below the 4.5:1 text minimum, dark mode even below the 3:1 UI-component floor) with a dark `text-shadow`/icon `drop-shadow` glow standing in for the missing contrast margin. Documented as a real, flagged exception in `/about/brand-guide/`'s Accessibility notes table (a "Fail (mitigated)" row, not rounded up to a false Pass), the same "flag, don't silently pass" treatment that page already gives `--cw-danger` and the `:::warning`/amber conflict.
- `SpeedLimitSign` — simplified MUTCD R2-1 sign, hardcoded black-on-white (not theme tokens, a real regulatory sign is legally black/white regardless of dark mode, same reasoning as `EvidenceChip`'s fixed fills).

**Documentation added to match:** How to Read a Pattern gained a "The Speed & Modal Hierarchy card" section (between Density Tiers and Admonitions) with a live rendered demo — a small fictional Local example built from the real components, not a real pattern's own data, matching the existing Pattern ID card demo's own precedent for staying decoupled from real content that can change later (a deliberate choice flagged to the user, since the alternative — reusing a real page's card — was also viable). Brand Guide's Components section gained two entries, "Speed & Modal Hierarchy card" (two rendered examples, Local side-by-side and Arterial stacked, plus all 5 named layout variations in prose) and "Modal hierarchy pills" (rendered `>` and `=` examples), dated via a third top-of-page `:::note` alongside the two existing ones, plus 6 new Accessibility notes rows with contrast ratios recomputed directly from live `tokens.css` values rather than trusted from the ticket.

## Retrofit Strategies Batch 1 scaffolding (2026.09.03)

**No Retrofit Strategy entries exist yet.** This batch only builds the pages Retrofit Strategy entries will cross-link to once Batch 2 adds the catalog itself: four new Make It Happen pages plus one new Foundations page, sourced from a written ticket (an instructions file plus five separate content files, one per page). Five real content pages landed, all under `src/routes/guide/` with flat slugs (not nested under `guide/make-it-happen/`, only the sidebar grouping is nested):

- **Legal & Planning Mechanisms** (`guide/legal-planning-mechanisms/`), **Funding & Financing Mechanisms** (`guide/funding-financing-mechanisms/`), **Decision & Evaluation Processes** (`guide/decision-evaluation-processes/`), **Construction Methodology & The Build Spectrum** (`guide/construction-methodology-spectrum/`) — four tool/process catalogs, added to `sidebar['/guide/']`'s **Make It Happen** group after the two existing pages (How Road Changes Actually Happen, When Jurisdiction Doesn't Follow the Pattern).
- **How to read a retrofit strategy** (`guide/how-to-read-a-retrofit-strategy/`) — the sibling instructional page to How to Read a Pattern, explaining the future `RFT-SCALE-##` Retrofit Strategy ID format (SCALE is CDR/INT/NET) and each section a Retrofit Strategy entry will carry, once real entries exist. **Placed in Foundations, directly under How to Read a Pattern, not in Make It Happen** — it was initially scaffolded into Make It Happen alongside its four batch-mates (matching the ticket's own suggested nav order literally), then corrected after the user flagged it: despite shipping in the same batch as the Make It Happen tool catalogs, it's structurally a sibling to How to Read a Pattern (an explain-the-format page, not a tool or process a jurisdiction runs), and `PageNav.svelte`'s prev/next pagination follows sidebar position, so this placement also determines what a reader is routed to next. **If a future batch ships a page that's topically adjacent to one group but structurally a sibling of a page in another, sidebar placement should follow the structural role, not the batch it shipped in or the ticket's own suggested nav order.**
- Sentence-case title on this one page only (`How to read a retrofit strategy`, both frontmatter `title` and sidebar label), deliberately not Title Case like every other page in this batch, to visually pair with its sibling's own sidebar label ("How to read a pattern" is sentence-case in the sidebar despite a Title Case frontmatter `title`/`<h1>`; this new page uses sentence case in both places instead, since the ticket specified the pairing at the sidebar level and there was no strong reason to split the two).

**Cross-references added both directions:** How to Read a Pattern's "Related patterns" section gained a closing pointer to How to read a retrofit strategy. How Road Changes Actually Happen (`guide/make-it-happen/+page.md`) gained three inline pointers at its existing natural attachment points, rather than a bolted-on "see also" list: its intro's corridor-preservation aside now links to Legal & Planning Mechanisms, its Question 4b engineering-study/warrant discussion now links to Decision & Evaluation Processes (ICE/RSA), and its Question 4c capital-project discussion now links to both Funding & Financing Mechanisms and Construction Methodology & The Build Spectrum.

**Ticket-verification judgment calls made this batch** (see the standing "verify a ticket's claims against the actual code" rule below): the four catalog pages' source content carried References lists with italic evidence-tier labels (*Legal/regulatory*, *Evidence-based*, *Precedent-based*) but no inline `<Citation>` markers anywhere in the prose — translated the labels to real `EvidenceChip` `tier` values (`legal`/`evidence`/`precedent`, confirmed against `EvidenceChip.svelte`'s own type, not the ticket's prose labels), and added inline citation badges myself at the most topically relevant claim for each numbered reference, so every citation has at least one inline attachment point rather than sitting orphaned in the References list (same "closest topical match" standard already used elsewhere, see the Citations note above). Both are straightforward authoring-syntax translations, not content decisions.

**Still to come in Batch 2, not yet built:** the actual Retrofit Strategy catalog entries themselves, and a new "Getting there: Retrofit path options" section on the pattern page template (which needs real Retrofit Strategy entries to link to before it has anything to show — deliberately not stubbed in ahead of that content, same reasoning as the Illustration section rule above).

## Retrofit Strategies Batch 2: the catalog itself (2026.09.03)

**24 new pages, sourced from a second written ticket (an instructions file plus 23 content files).** This batch builds the actual Retrofit Strategy catalog Batch 1 only scaffolded around, plus six new patterns the catalog and a site audit required, plus the reciprocal cross-referencing across both catalogs. `RFT-SCALE-##` entries are real now: 10 Corridor (`RFT-CDR-01` through `RFT-CDR-10`), 4 Intersection (`RFT-INT-01` through `RFT-INT-04`), 3 Network (`RFT-NET-01` through `RFT-NET-03`).

**New top-level section, `/retrofits/`, sibling to `/patterns/` in the navbar** (`vite.config.ts`'s navbar array, between Patterns and Reference). Nav label and page slugs use **Retrofits**; page titles and prose use **Retrofit Strategies**, per the naming decision Batch 1 already documented. `sidebar['/retrofits/']` has an ungrouped index entry (`Retrofit Strategy Index`, same "front page listed first" convention as `/guide/`'s Start Here and `/about/`'s Overview) plus three named, collapsible groups — Corridor, Intersection, Network — mirroring `/patterns/`'s own group-per-scale shape. `src/routes/retrofits/{corridor,intersection,network}/{slug}/+page.md`, one folder per strategy, plus a bare-root `src/routes/retrofits/+page.md` index page (a plain numbered-list page, not a card grid like the Pattern Index, since there's no illustration/placeholder-image system for Retrofit Strategies to justify one).

**New component, `RetrofitCard.svelte`** (`src/lib/theme/components/`), not a reuse of `PatternCard`: same visual language (ID in monospace top-left, a right-aligned label, a title, an optional body), but a `scale` prop (`corridor | intersection | network`, matching the ID's own SCALE segment exactly, labeled Corridor/Intersection/Network) instead of `classification`, and **no status prop at all** — Retrofit Strategies never carry a Recommended/Situational/Avoid badge, per "How to Read a Retrofit Strategy"'s own explanation (a strategy isn't endorsed or discouraged, it's either applicable or it isn't). Reusing `PatternCard` and simply omitting `status` was considered and rejected: `PatternCard`'s `classification` union has no `'intersection'` value (only the plural `'intersections'`, meaning Intersections & Crossings the Pattern classification, a different vocabulary from Retrofit Strategy's singular `INT` scale), so a dedicated component keeps the two ID schemes from bleeding into each other. **PLACEHOLDER, needs a real color once assigned:** all three scales currently reuse Freeway's neutral-gray ramp as the card's left-border accent, the same open item as `PatternCard`'s four newest classifications (network/corridor/facility/element).

**Six new patterns landed alongside the catalog**, three Element and three Intersections & Crossings, because the Retrofit Strategies themselves needed real destination patterns that didn't exist yet:

- **Element** (`sidebar['/patterns/']`'s Elements group, appended in sequential order): Modal Filter / Point Closure (`ELM-MFP-44`), Median Island (`ELM-MDI-45`), Neighborhood Traffic Circle / Mini-Roundabout (`ELM-NTC-46`). All three carry `status="recommended"` and the same "Working assessment" preliminary-judgment-call note as every other Element pattern.
- **Intersections & Crossings** (appended after Protected Intersection): Median U-Turn / MUT (`INT-MUT-12`), Restricted Crossing U-Turn / RCUT (`INT-RCU-13`), Displaced Left-Turn / DLT (`INT-DLT-14`). **All three deliberately carry `status="situational"`, not Recommended** — each is a strong fit for a narrow context (high-speed divided roads for MUT and RCUT, high-volume urban/suburban intersections for DLT) rather than a broad default, per the ticket's explicit instruction; do not "fix" these to Recommended on the assumption that a new pattern should default to it.
- All six patterns are also live in `src/routes/patterns/+page.md`'s card grid (111 patterns total now, up from 105 post-Stroad-merge — see `release-history`'s own live `patternPageCount` computation for the current true count rather than trusting this number as still current) and, where the catalog names one, carry a `## Getting there: Retrofit path options` section of their own pointing back at the strategy that produces them (Modal Filter → `RFT-NET-01`, Median U-Turn → `RFT-INT-02`, RCUT → `RFT-INT-03`, DLT → `RFT-INT-04`).

**New pattern-page template section: `## Getting there: Retrofit path options`, placed immediately after `## Related patterns` and before `## References`** (the same slot on every page that has one, matching the placement already established on Boulevard/Roundabout during scaffolding-adjacent work). Populated on 22 existing pattern pages per the ticket's mapping — every Collector/Arterial Default, Boulevard, Downtown Thoroughfare, Main Street, Bicycle Boulevard, the three Transit Typologies, Bike Highway/Greenway, Protected Bike Lane/Cycle Track, Rural Collector, Roundabout, Turbo-Roundabout, the three new Intersection patterns, Living Street/Woonerf, Pedestrian-only Street/Plaza, Shared-Use Path, and Multi-Use Trail/Greenway. This is now a standard section for any future pattern page with a real Retrofit Strategy leading to it; omit it (don't add an empty shell) on a pattern nothing currently retrofits into, same "an absent section is more honest than an empty one" rule the Illustration section already established.

**Network-scale phrasing, per the ticket's own instruction:** unlike every Corridor and Intersection strategy, the three Network-scale strategies (`RFT-NET-01/02/03`) don't produce a different destination pattern, they change a property (permeability, interior-street allocation, connectivity) of the same pattern or topology they're applied within. Rather than inventing a separate heading for this (which would break the mirror-image relationship "How to Read a Retrofit Strategy" describes between a Retrofit Strategy's "Where this leads" section and a Pattern's "Getting there" section), the heading text stayed `## Where this leads: Target patterns` / `## Getting there: Retrofit path options` on all three, with the prose itself saying "applies *within*" rather than "leads to." Network topology patterns themselves (Grid, Dendritic/Hierarchical) were **not** given a "Getting there" entry, per the ticket: the effect of a Network-scale retrofit shows up on the Local-scale patterns it produces (Bicycle Boulevard, Living Street/Woonerf, Pedestrian-only Street/Plaza, Shared-Use Path, Multi-Use Trail/Greenway) instead.

**Reciprocal Related Patterns edits, 10 existing pages** (Bollard, Curb Extension, Bicycle Boulevard, Pedestrian Safety Island/Refuge, Boulevard, Roundabout, All-Way Stop, Vertical Speed Control, Signal, Grade-Separated Interchange), each gaining a link back to whichever new pattern(s) already link to it, per the ticket's explicit mapping (Section 2). Signal's edit is the only one with three added links at once (Median U-Turn, RCUT, and DLT all modify Signal rather than replace it).

**JSON-LD schema (`src/lib/theme/schema/page.ts`) extended for the new section:** `/retrofits/` (the index) gets the same `CollectionPage`/`ItemList` treatment `/patterns/` already has, sourced from `sidebar['/retrofits/']` via the existing `flattenPatternLinks()` helper (already generic over any sidebar shape, no changes needed there); `/retrofits/*` pages get `TechArticle` with `identifier: fm.patternId` and an `about: DefinedTerm` pointing at `/retrofits/`, mirroring pattern pages' own treatment exactly. `llms.txt`'s `SECTION_LABELS` map gained a `retrofits: 'Retrofits'` entry, matching the navbar label convention every other section already follows.

**Verified against a real `pnpm run build`, not just `pnpm run check`:** all 24 new pages' internal links (target patterns, cross-linked Retrofit Strategies, Guide tool-catalog links, `<Citation>`/`<span id="ref-N">` anchor pairs) resolved cleanly under `adapter-static`'s `strict: true` prerendering on the first build, no broken-link fixes needed. Visually spot-checked in the dev server: `RetrofitCard` renders correctly (ID/scale header, no status badge, correct classification-color placeholder), the new `/retrofits/` sidebar and nav entry work, and Bicycle Boulevard's combined reciprocal-link-plus-"Getting there" edit renders both new sections cleanly.

**Ticket-verification judgment call made this batch:** the three protected bike lane retrofits' "Where this leads" sections mix a Segment-scale Pattern ID with an Element-scale one (e.g. "Default (Standard Collector) (COL-DEF-01) with Protected Bike Lane / Cycle Track (ELM-PBL-37)"), since the retrofit's actual physical product is the bike-lane Element added to an existing Segment pattern, not a wholesale change of the street's own Typology. The ticket flagged this as worth confirming rather than assuming; it renders as plain prose with two links and reads sensibly, no special markup needed.

**Follow-up (same day): `/retrofits/` rebuilt as a card grid, plus a `RetrofitCard` Brand Guide entry.** The index page originally shipped as a plain numbered list (matching the source ticket's own `retrofit-strategy-index.md`); the user asked for it to match the Pattern Index's card-grid treatment instead. New component, `RetrofitIndexCard.svelte`, mirrors `PatternIndexCard.svelte` exactly (same shell, hover, overlay mechanics, shared placeholder image, per-scale number circle restarting at 1) but takes a `scale` prop instead of `classification` and never renders a status badge. **Coloring rule, per explicit user instruction ("share the same coloring as their peers on the Pattern Index page... based on scale or functionality"):** each Retrofit Strategy scale borrows the color of whichever Pattern classification is its functional peer, rather than inventing a separate Retrofit-only palette — Intersection strategies get the real Intersections & Crossings ramp (their Pattern peers, Intersections & Crossings, already have one), while Corridor and Network strategies inherit the Freeway-gray placeholder, matching Patterns' own still-placeholder "corridor" and "network" classifications. `RetrofitCard.svelte` (the individual-page ID card) was updated to the same rule for visual consistency between the index card and the page it links to, even though only the index card was explicitly requested — a `data-scale='intersection'` override now points its left-border color at `var(--cw-intersections)`, the same representative token `PatternCard` uses for that classification; Corridor and Network stay on the Freeway placeholder. Both new components are now documented on `/about/brand-guide/` under Components ("Retrofit Strategy card" and "Retrofit Strategy Index card"), each with rendered examples covering a Corridor/Intersection (and, for the index card, Network) example so the color mapping is visible, not just described. **The claim that the six new patterns were missing from the Pattern Index turned out to be incorrect** — all six (`ELM-MFP-44`, `ELM-MDI-45`, `ELM-NTC-46`, `INT-MUT-12`, `INT-RCU-13`, `INT-DLT-14`) were already added to `src/routes/patterns/+page.md` during the original Batch 2 work; re-verified live in the dev server rather than assumed, no fix was actually needed there.

## Media Gallery & Related Media (2026.09.04)

**A new content shape, distinct from Patterns/Retrofit Strategies/Guide pages, built for this feature: data records instead of full content pages.** Patterns, Retrofit Strategies, and Guide pages are each a hand-authored `+page.md` with real prose and their own route; there is no queryable collection behind them, the "index" pages are hand-typed lists of card components. Media items have no route of their own and need to be filtered/sorted/cross-referenced, so they're stored as one TypeScript file per entry under `src/lib/data/media/entries/` (e.g. `med-001.ts`, exporting a `default` object typed against `MediaItem` in `src/lib/data/media/types.ts`), globbed at build time by `src/lib/data/media/index.ts` (`getMediaItems()`, `getMediaItemsFor(pageId)`) the same `import.meta.glob` mechanism `$lib/server/content.ts` and `$lib/theme/blog.ts` already use for `+page.md` frontmatter. `.ts` over `.json`: identical build-time-only performance either way, but `.ts` gets checked by `pnpm run check` (catches a typo'd `endorsementStatus` or a missing field at edit time; JSON has no schema enforcement). **This is the pattern to reuse for any future data-record content type** (not a full page) that needs filtering or cross-referencing, the same way Patterns/Retrofits are the pattern to reuse for a future full-page content type.

**`MediaItem` schema** (`src/lib/data/media/types.ts`): `id` (permanent, e.g. `MED-001`, never reused), `type` (`video | podcast | article`), `title`, `url`, `sourceName`, `sourceIcon` (optional — full external URL to hotlink, or a local `/`-prefixed static path; omit for a generic Lucide fallback icon keyed to `type`, see `MediaCard.svelte`), `image` (optional, same dual-sourcing rule; omit for the shared placeholder `static/media-assets/media-placeholder.svg`), `blurb` (how the item relates to CS content, not a neutral summary), `endorsementStatus` (`aligned | context-only | counterexample`, always rendered as a uniform neutral pill with no color-coding, a deliberate house rule), `patternTaxonomyTags` (reuses the Local/Collector/Arterial/Freeway vocabulary), `mediaTags` (locked vocabulary: `Case Study`, `Explainer`, `Interview`, `News/Advocacy`, `Historical` — do not add a value without confirming first), `relatedTo`, `dateAdded` (ISO 8601). 20 entries exist as of 2026.09.04, added across three batches: `MED-001` (Not Just Bikes, continuous sidewalks); `MED-002`–`MED-005` (City Beautiful ×3, BicycleDutch ×1); `MED-006`–`MED-020` (Global Cycling Network, Not Just Bikes, City Beautiful, Safe By Design, Streetcraft, Road Guy Rob) — see the batch notes below for what each round involved.

**`relatedTo` accepts Pattern IDs, Retrofit Strategy IDs, *and* trailing-slashed route paths** (e.g. `/guide/legal-planning-mechanisms/`) in the same array — extended beyond the original ticket's "Pattern IDs and/or Retrofit Strategy IDs only" spec once it became clear Related Media was also supposed to render on Guide/About pages, which have no ID of their own. When adding a future media item tagged to a Guide or About page, use that page's own route path in `relatedTo`, not an invented ID.

**Two ticket-verification gaps found and resolved before implementation** (see the standing ticket-verification rule under Working conventions): (1) the ticket said to "reuse the existing Form Controls component pattern... already used on the Pattern Index" — no such thing exists. `FormControlsPreview.svelte` is an explicitly decorative, non-functional mockup (its own code comment says "Not wired to real filtering") shown only on the Brand Guide page; the real Pattern Index has no search/filter/sort at all. The Media Gallery's filtering (`MediaFilters.svelte`) is real, newly-built client-side logic, not a reuse of anything. (2) See the `relatedTo` note above for the Guide/About gap.

**Components** (`src/lib/theme/components/`): `MediaCard.svelte` (the card itself — featured image with a media-type icon overlay and an endorsement-status pill, both on solid `--cw-ink-fixed`/`--cw-paper-fixed` tokens rather than a translucent overlay, since a translucent fill's contrast would depend on whatever photo sits underneath, the same guaranteed-contrast reasoning as `PatternIndexCard`'s number circle; title; source line with its own icon-or-fallback; blurb; whole card is one external link with the site's standard `externalLinkAttrs`/opens-in-new-tab treatment applied by hand, since this is a hardcoded link from data, not markdown, same as `Navbar`/`SidebarGroup`/`PageLayout`'s edit link). `MediaGallery.svelte` (the `/media/` results grid + count, 3-column, using the shared `.pattern-index-grid` class). `MediaFilters.svelte` (the search/checkbox/sort control panel, laid out as a vertical stack). `RelatedMedia.svelte` (the boxed, self-hiding section — renders nothing at all if no media item's `relatedTo` matches the given page id — used on Pattern pages, Retrofit Strategy pages, and Guide Foundations/Make It Happen/`about/commonway-system/` pages; its card grid is its own `.related-media__grid` class, deliberately **not** `.pattern-index-grid`, capped at 2 columns rather than 3, a later explicit user request to keep the Gallery's own 3-column grid untouched while narrowing the boxed callout's grid). Icons are 3 more Lucide icons (circle-play, headphones, file-text, plus mic and video as source-icon fallbacks), same ISC-licensed inline-`{@html}` convention as `ModalHierarchyPill.svelte`; see `/THIRD-PARTY-NOTICES`.

**`/media/` uses a custom 2-column layout, not the sitewide 3-column sidebar/content/toc shell** — a later, explicit user request. `GlobalLayout.svelte` gains an `isMedia` route check (`page.url.pathname === '/media/'`, same mechanism as the homepage's existing `isHome`), which hides the sidebar (`.shell--media :global(.sidebar) { display: none }`, same technique `.shell--full` already uses for the homepage) and swaps the toc-column slot from `<Toc />` to `<MediaFilters variant="desktop" />`. The content column absorbs the sidebar's freed width (`grid-template-areas: 'content toc'` with only 2 tracks instead of 3), and `/media/+page.md` sets `wide: true` so `.cw-content` actually uses the wider column instead of staying capped at the 46rem prose measure. **Because `MediaFilters` (in the toc grid slot) and `MediaGallery` (in the content grid slot) are rendered as separate siblings by `GlobalLayout`, not nested,** they can't share reactive state through props — filter state (search text, selected type/source/topic checkboxes, sort order) lives in a new `src/lib/theme/media-filters.ts` module of `writable` stores, the same cross-component-state mechanism `layout.ts` already uses for `sidebarOpen`/`pageAnchors`/`isDark`, not Svelte 5 runes. If a future page needs a similarly custom (non sidebar/content/toc) shell, say so explicitly rather than assuming Claude will infer it — `isHome` and `isMedia` are the only two departures from the default 3-column template so far, and both required a `GlobalLayout.svelte` change, not just a page-level one.

**`MediaFilters.svelte` mobile redesign (2026.09.04, two follow-up rounds after initial ship):** the desktop 2-column layout above is only half the story below 940px. Round 1 found a real bug: the original single always-`position: sticky` implementation stayed pinned to the top of the viewport in the mobile stacked layout too, so scrolling made it visually overlap the results grid scrolling underneath it (sticky only makes sense next to a much taller sibling column, the desktop case). Round 2 was a design request: a permanently-expanded filter panel was also eating most of the mobile screen before any results were visible, and the user wanted it collapsed by default on mobile with no toggle at all on desktop, repositioned to sit just before the results count/grid rather than above the page title.

The fix ended up as **two small mounts of the same component, not one instance repositioned with CSS**, via a `variant: 'desktop' | 'mobile'` prop: `variant="desktop"` renders a plain `<aside>`, always expanded, no toggle affordance, CSS-hidden below 940px — this is what `GlobalLayout.svelte` mounts in the toc-column slot, unchanged in spirit from the original design. `variant="mobile"` renders a real `<details>`/`<summary>` disclosure, collapsed by default (no `open` attribute, works with no JS via native browser semantics), CSS-hidden at 940px and up — this is mounted separately, inline in `/media/+page.md`'s own markdown between the intro paragraph and `<MediaGallery />`, landing it exactly where a reader expects a filter control: right before the results, not above the title. Both instances read/write the same `media-filters.ts` stores, so they're never out of sync, just two different rendering locations for the same underlying state. **The reason it's two mounts instead of one repositioned element:** the desktop slot (a `GlobalLayout`-level grid sibling) and the mobile position (a descendant inside the page's own content column, itself nested in a completely different grid container) are genuinely different DOM subtrees — no CSS mechanism (`order`, `grid-area`, positioning) moves an element between two unrelated grid contexts. If a future page needs "different content/position per breakpoint, sharing one piece of state," this two-small-mounts-plus-shared-store pattern is the one to reuse, not a single repositioned element.

**MED-006 through MED-020 (2026.09.04, third batch):** 15 more entries, sourced from a batch explicitly marked "draft, not a ticket yet" — every pattern/retrofit reference in it was a `[CONFIRM ID]` placeholder (a name only, no ID code) that needed resolving against real frontmatter, and several items were flagged by the draft's own author for a judgment call rather than decided in the draft itself. Resolution method: `grep -rhoE "^patternId: \S+"` across `src/routes/patterns` and `src/routes/retrofits` to build a title→ID lookup table, same mechanical check as the MED-002–005 batch. Four judgment calls were surfaced to the user rather than decided unilaterally (all approved as drafted): MED-006 (no clean pattern fit, a Vision Zero/dangerous-drivers video) tagged to `/about/commonway-system/`, the only page that actually discusses Vision Zero and CS's philosophical grounding; MED-011 (stroads explainer covering the Dutch 3-tier classification) additionally tagged to `/guide/street-types/`; MED-014's `endorsementStatus` set to `context-only` rather than `aligned`, since the source video itself reports the turbo roundabout's documented cyclist/pedestrian crossing hazards, not just its benefits; MED-013's suggested "new rural highway safety Retrofit Strategy" was explicitly held off as its own future ticket rather than folded into a media-only batch. Two additional `relatedTo` entries were added on Claude's own initiative, not from the draft's placeholders, both well-supported by the video's own described content: `RFT-CDR-05` (Parking-lane reallocation) on MED-009, and `RFT-CDR-01` (Four-to-three lane road diet) on MED-016. `sourceIcon` was deliberately left unset for the batch's 4 new channels (Global Cycling Network, Safe By Design, Streetcraft, Road Guy Rob) rather than guessing a plausible-looking URL, since the draft never actually supplied one despite claiming "all sourceIcon URLs use the channel avatar CDN pattern" — real URLs for all 4 were supplied in a follow-up message and added after the fact. **Same standing lesson as MED-002–005: verify a draft's claims (here, "the URLs are all included") against what's actually present in the file, not the file's own description of itself.**

**One direct fix, no batch:** MED-002 (cul-de-sac video) was missing `LOC-CDS-03` (Cul-de-sac) from its own `relatedTo` array despite the page's own title being about cul-de-sacs specifically — added on a one-line user request, unrelated to any batch.

**Adding a new media item going forward:** create one new file under `src/lib/data/media/entries/`, filled in against the `MediaItem` schema above (all fields the same as the ticket originally specified, `sourceIcon` and `image` both optional/dual-sourced, `relatedTo` accepting route paths too now). No other file needs to change, `getMediaItems()`/`getMediaItemsFor()` pick it up automatically via the glob.

**MED-002 through MED-005 (2026.09.04, second batch, no schema/component changes):** four more entries, all `type: video`, all `endorsementStatus: aligned`, all `mediaTags: ['Explainer']`. This batch's ticket asked to confirm every `relatedTo` ID (47 total across the four entries) resolves to a real, live page before implementing, rather than trusting the list as given — the actual check: `grep -rhoE "^patternId: \S+" src/routes/patterns src/routes/retrofits` to build the master list of every live ID, then diff each entry's `relatedTo` array against it. All 47 resolved; nothing needed flagging. **Worth repeating for any future batch with more than a couple of `relatedTo` IDs** — it's a cheap, mechanical check and the ticket's own author (a separate Claude session with no live access to this repo) can't run it themselves. Separately, this ticket's stated dev-server port (`localhost:4000`) didn't match the project's real one (`5173`, see Commands above) — a small, harmless mismatch, verification just used the real port instead, but it's the same class of thing as the Form Controls/`relatedTo` gaps from the first batch: content drafted in a separate Claude session with no live access to this repo, working from a stale or assumed mental model of it rather than the actual code. The user's standing mitigation: after a batch like this, they ask for a short plain-text summary of what actually got built/decided/fixed, which they paste back into that other session so its next ticket starts from an accurate picture instead of repeating the same class of mismatch.

## Content model notes

- **`PageLayout.svelte` renders an `<h1>` from the page's frontmatter `title` automatically** (see `{#if fm.title}<h1>{fm.title}</h1>{/if}` in `PageLayout.svelte`). Do not start a `+page.md` body with a `# Heading` that repeats the title, it duplicates the h1. Existing pages (`guide/` bare root, `guide/how-to-read-a-pattern`, etc.) go straight from frontmatter into body text or an `##` subheading; follow that pattern for new pages.
- A companion brand guide (`../commonway-brand-guide.html`, outside this repo) defines the real, locked CS visual system: base colors (ink `#141414`, paper `#FDFAF3`, teal `#00A896`, indigo `#3D3AAB`, reserved amber `#FF9F1C`), a colorblind-checked 6-step Functional Classification × density ramp, and the Fraunces/Inter type scale. `tokens.css` is synced to it (see above); the guide itself doesn't cover dark mode, so every dark-mode value in `tokens.css` is a site-only extension, reasoned out and documented inline rather than specified by the guide.
- CS editorial rules that apply to any `+page.md` content written in this repo: no em-dashes anywhere, plain-language term introduced before its formal/technical name, and citations follow a specific numbered/dated agency-reference format (see the brand guide for the full citation and evidence-tier system referenced by `Citation.svelte` and the admonition tiers).
- The mobile hamburger drawer (`Sidebar.svelte`, `<940px`) shows the navbar's own primary links (Home/Guide/Patterns/Reference/Blog/About) above the current page's sidebar tree, not just the tree. Both live in one `<aside>` now; the primary-nav block is CSS-hidden `≥940px` since the navbar shows those links directly there. Before this fix the drawer only rendered when the current route had a page-specific sidebar, so the navbar's own links were completely unreachable on any narrow viewport, including the homepage.

## Repo-root contributor infrastructure

`LICENSE` (MIT), `CODE_OF_CONDUCT.md`, `.github/PULL_REQUEST_TEMPLATE.md`, and `.github/ISSUE_TEMPLATE/*.yml` (factual-error, missing-citation, dead-link, accessibility-issue, plus `config.yml`) are real files backing `/about/contributing/`, `/about/governance/`, and `/about/reporting/` — those pages link to them by their GitHub blob URL, so keep the page's prose and the template/file itself in sync if the process changes. **Two different licenses cover two different things**, documented in both `README.md` and `/about/terms-and-conditions/`: `LICENSE`'s MIT covers the site's code only; the guide content itself (the pattern pages and prose) is licensed separately per the "Terms of contributing" section on `/about/contributing/`, an entity-retains-rights / contributor-grants-a-license model, not MIT and not Creative Commons. GitHub's own license detector only surfaces one license per repo, so this split has to be read, not assumed from the repo's own "License" badge.

**`THIRD-PARTY-NOTICES`** (added 2026.09.02, repo root, no extension, matching `LICENSE`'s own convention): holds license text for third-party assets embedded directly as code rather than pulled in as a `package.json` dependency — currently just the 5 Lucide icons in `ModalHierarchyPill.svelte` (ISC license, verbatim text fetched live from Lucide's own repo rather than trusted from memory). Linked from `/about/terms-and-conditions/`'s "Code and content licensing" section the same way that section already links to `LICENSE`. Add a new entry here, not to `LICENSE` itself, any time a future component embeds another third party's code/icon/asset directly rather than through a package dependency.

## Deployment

Live at **commonwaysystem.org**. Domain registered and DNS/SSL-proxied through **Cloudflare** (SSL/TLS mode: Full (strict); proxy "orange cloud" on — Netlify's own SSL/domain settings are otherwise untouched, they just need *a* valid cert for the Cloudflare→Netlify leg). Hosted on **Netlify**, building from `netlify.toml` (`pnpm run build`, publish `build`, `NODE_VERSION=24`). Source repo is **[github.com/Commonway-System/commonwaysystem.org](https://github.com/Commonway-System/commonwaysystem.org)** (public), pushed to `main`; the `github`/`editLink` theme options in `vite.config.ts` point at it. No CI beyond Netlify's own build-on-push; there is no separate staging environment or manual deploy step, a push to `main` is a publish.

## Known gaps (deliberately unresolved, not bugs to silently fix)

These surfaced across several sessions and are documented here plus inline (in `tokens.css` and on `/about/brand-guide/`) so they don't need rediscovering. Each is a real design/content-semantics call, not a clear-cut fix, raise it with the user rather than deciding unilaterally:

- ~~No Retrofit Strategy entries exist yet~~ **Resolved 2026.09.03, Batch 2:** all 17 `RFT-SCALE-##` entries are real pages now, under a new top-level `/retrofits/` section with its own index, and the pattern-page template's "Getting there: Retrofit path options" section is populated on 22 pattern pages. See the "Retrofit Strategies Batch 2" note above for the full build.
- The `:::warning` admonition (`src/lib/theme/markdown/admonitions.ts`) uses `--cw-amber`, which conflicts with the Brand Guide's explicit "amber is reserved exclusively for the unsourced-claim flag and pattern highlighting, never a general UI accent" rule. Not changed since there's no guide-specified alternative warning color.
- `DensityChip.svelte` renders density tiers on its own independent neutral-tan ramp. The real Brand Guide design encodes density _within_ each Functional Classification's own 6-step color ramp instead (see `tokens.css`'s `--cw-local-undeveloped` through `--cw-local-core`, etc.), which would mean `DensityChip` taking a `classification` prop too. Not implemented, current behavior is a deliberate site-only choice per the component's own comment.
- `--cw-danger` / `--cw-danger-soft` in `tokens.css` are marked PLACEHOLDER: the guide has no danger-admonition color at all, so this stays an independent site-only red. (`--cw-intersections` was the other placeholder here; resolved 2026.08.30 with a full site-only ramp, see the Brand tokens note above, itself flagging a real hue-proximity-to-amber tension rather than resolving it.)
- `guide/find-your-pattern` is still a deliberate placeholder page, not a stub awaiting a wholesale rewrite: it already carries real explanatory copy about what it will become, plus the standing `:::tip` "planned but not yet written" callout, the same shape used for the site's other genuine placeholders. It holds exactly one sidebar entry under its own named group on purpose (`Find Your Pattern` in `vite.config.ts`'s `sidebar['/guide/']`): expected to split into a short diagnostic sequence later, and the group already exists so nothing has to move in the sidebar tree when that happens, only new items get added to the group's `items` array.
- **`guide/make-it-happen` is no longer a placeholder (real content landed 2026.09.01).** It's now "How Road Changes Actually Happen," a real four-question method (what you're trying to do; public vs. private road; which government has authority; how that government processes the request) with 16 real citations, plus a companion reference page, `guide/make-it-happen/jurisdiction-variants/` ("When Jurisdiction Doesn't Follow the Pattern," documented exceptions like consolidated city-counties, independent cities, and interstate compact authorities). Both are real sidebar entries under the `Make It Happen` group now, not the single placeholder item the group used to hold. This resolves the "Process-explainer chapter" line that used to sit in `/about/release-history/`'s Roadmap.
- There are no Local/Collector/Arterial/Freeway classification-level landing pages yet within `/patterns/` — a site-structure decision the user has explicitly deferred, not forgotten. (Full citation sourcing, formerly listed here too, was completed 2026.09.01 — see the Citations note below.)
- `/about/brand-guide/`'s "Urban main street" PatternCard demo uses `ART-UMS-09` as its illustrative ID (renumbered 2026.08.31, was `ART-UMS-07`): the predicted collision this note used to flag actually happened once Commercial Arterial (`ART-CAR-07`) and Parkway (`ART-PKY-07`, see below) became real 7th/8th Arterial Typologies in the 2026.08.31 batch, so the demo moved to the next unused number. **If a real 9th Arterial Typology is ever added, it will collide with this demo ID again** — same open issue, new number.
- **`ART-PKY-08` renumbered to `ART-PKY-07` (2026.09.02), a one-time, explicitly authorized exception to the "Pattern ID numbers are assigned once and never reassigned" rule stated above.** Commercial Arterial's retirement (see the Stroad merge note below) had removed the Arterial table's row 6, leaving the Pattern Index numbered 1, 2, 3, 4, 5, 6, **8** with no row 7, since `PatternIndexCard`'s number badge is derived directly from the ID's own trailing digits, not a separate listing-position prop (see `PatternIndexCard.svelte`). Closing that visible gap meant either renumbering Parkway's ID or adding a decoupled listing-number prop to the component; the user chose renumbering, reasoning that the site is four days old, not yet seen by anyone else, and still in early development, so the usual "never renumber a shipped ID" rationale (a real reader may already have bookmarked or cited the old ID) doesn't yet apply. This does not reopen the rule generally: once the site has real readers, an ID gap from a future retirement should stay a gap (or get a decoupled listing-number prop) rather than triggering another renumbering.
- ~~Organic/Irregular's status-badge treatment~~ **Resolved 2026.09.02:** a fourth `StatusBadge`/`PatternCard`/`PatternIndexCard` status value, `descriptive`, was added (label "Descriptive", `--cw-descriptive-ink` text on the existing `--cw-primary-soft` background — see `tokens.css`, a theme-aware ink token rather than a fixed hex since the dark-mode `--cw-primary-soft` is a dark teal that only cleared 1.99:1 against the ticket's literal fixed hex). Organic/Irregular (`NET-ORG-04`) now renders `status="descriptive"` and its old pending-decision `:::note` callout is gone. No other page currently qualifies; Cul-de-sac and Alley were audited and confirmed not to.
- **PLACEHOLDER, needs real colors:** `PatternCard`/`PatternIndexCard`'s four newest classifications (`network`, `corridor`, `facility`, `element`, added 2026.08.31) all reuse Freeway's neutral-gray ramp as a stand-in border/overlay color, since the Brand Guide doesn't define one for any of them yet. See the Pattern Index page note above.
- ~~Pattern page body structure is inconsistent~~ **`## Related patterns` coverage resolved 2026.09.02 (Tickets A, D, H): all 105 live pattern pages now carry exactly one.** The remaining inconsistency is narrower than before: on 62 pages (the Network/Corridor/Facility/Element batch plus the 3 Segment/Intersection additions from 2026.08.31), an italic `*Working assessment: this pattern's status reflects a preliminary Commonway System judgment call, pending final review.*` note still sits right under the Pattern ID card; the original ~40 pages and Bicycle Boulevard (rewritten 2026.09.02) don't carry it, since that framing is specific to the newer batch's preliminary judgment calls. Not reconciled: dropping the "working assessment" framing from the newer batch once those patterns get real review is an open follow-up, not an oversight to silently fix.
- **The `Illustration` section (component built and rule set 2026.09.02, see above) is authored on only 3 of 106 pattern pages** — Bicycle Boulevard, Local Default, and Stroad. It's now a required core section for any pattern page going forward, placed immediately after the Pattern ID card, but the other ~103 pages have no Illustration section at all yet: that content (frame descriptions, callouts, citation attachments) has to be drafted per pattern, it can't be generated from existing page text. Don't add a bare `<Illustration>` shell without real scene/callout content just to check the box; an absent section is more honest than an empty one.
## Working conventions on this repo

- **Visually verify UI changes in the browser before calling them done**, not just `npm run check` (which won't catch a badge rendering illegibly small, a color barely visible in dark mode, or a hidden character leaking into visible text, all real issues found this way in past sessions). Check both light and dark mode, and at least one narrow viewport width, since this theme has real behavior differences at 940px and 560/480px breakpoints.
- **After drafting page content from a written ticket/instruction file, re-read the rendered section specifically for sentences addressed to Claude rather than to a reader** (a real, confirmed failure mode, not hypothetical — see the Bicycle Boulevard leaked-note bug in the Ticket G-J note above). A ticket file mixes real page content with meta-commentary about that content ("this citation also appears in callout 3 above," "add this verbatim," section-placement notes); it's easy for a sentence explaining *where else* something appears to get pulled in as if it were prose for the page. A plain read-through of the finished section, asking "would a reader be confused by this sentence," catches it; `pnpm run check`/`pnpm run build` won't, since it's valid markdown that renders and links fine.
- **Compute contrast ratios rather than eyeballing color choices.** A short Python snippet (WCAG relative luminance formula) is fast and has repeatedly caught real problems eyeballing missed, e.g. the real Brand Guide amber turning out to be only 1.97:1 as a focus ring against light paper.
- When a fix surfaces an adjacent problem (a token value fix revealing a component that also needs to change, for instance), **fix genuine bugs and contrast failures directly**, but **flag pure design/content-semantics judgment calls** rather than deciding them, see "Known gaps" above for the current list of flagged-not-fixed items.
- This is a static site (`adapter-static`, deploying to Netlify). Anything touching routing, build output, or server-side code should be checked against that constraint, e.g. a new `+server.ts` endpoint needs `export const prerender = true` and, if nothing links to it, an explicit entry in `svelte.config.js`'s `kit.prerender.entries`, or it silently won't exist in the deployed build.
- **Before pushing to `main` (a push is a publish, see Deployment above), follow the publishing checklist described on `/about/release-history/`:** draft a short build note covering what content changed, what structural/site changes shipped, and anything left in a known-incomplete state, then use that note as both the commit message and a new entry in that page's `releases` array (newest first; a quiet day can stay a one-line `summary`, a heavier day can use a bulleted `items` list instead, per-entry, not a shape every entry is forced into). The site's CalVer version (shown in the navbar) is computed from the last commit's own date via `siteVersion()` in `src/routes/+layout.server.ts` (see that file's note above), so it tracks the push date rather than whenever Netlify happens to run a build; the changelog's own `releases[0]` should match what actually shipped that day, not be left stale.
- **`vite.config.ts`'s sidebar is a manual tree, `git status`/`git add` don't know about it**: adding a new page also means adding it to the relevant `sidebar['/...']` entry and, if it should show up in the Pattern Index, to `src/routes/patterns/+page.md`'s list too, or it exists but is unreachable via navigation.
- **On visual/landing-style work (the homepage build, its later redesign pass), the user wants real editorial polish, not a plain content-column render**, and will say so plainly if a first pass reads as flat or dated ("looks like an old-school 2000s site"). Concretely, this has meant: full-bleed sections that break out of the sidebar/toc grid and the 46rem prose column (see the `isHome` full-bleed pattern and the `wide` frontmatter flag, both in the Hero/EvidenceStrip/FeatureGrid note above), deliberate background contrast between sections rather than one flat page color, subtle motion (gated behind `prefers-reduced-motion`, never the only way to convey something), and named color/background treatments matching what a UI element is actually *for* (the differentiator strip's amber-to-teal swap: amber read as a warning, not a feature highlight) over "was it the closest existing token." When the user names reference sites (Vite's and NACTO's homepages were named once) for layout/presentation ideas, actually open them in the browser tool and look, rather than working from general recollection of what those sites are like — concrete, current specifics (their two-column hero layout, glow/depth treatment, full-bleed bands) drove real decisions here that a vague memory wouldn't have.
- **Pushing to GitHub needs `GITHUB_TOKEN` from `.env`** (gitignored, never commit it): the `origin` remote is HTTPS (`https://github.com/Commonway-System/commonwaysystem.org.git`), not SSH, so a plain `git push` has no credential source of its own in a fresh environment. Use the token for that one push rather than assuming an SSH key or a cached credential helper is present.
- **Moving or renaming a whole route directory isn't just a file move and a link-text find/replace.** The sitewide `guide/` → `patterns/` restructure broke a page that no sitewide link grep would have caught: `src/routes/about/release-history/+page.md`'s own `<script>` block ran `import.meta.glob('/src/routes/guide/**/+page.md')` to compute a live pattern-page count, and silently started returning 0 once the pattern pages moved out from under `guide/` (no build error, no broken link, just a wrong number rendered on a live page). When restructuring routes, grep for `import.meta.glob` calls anywhere in `src/` (not just markdown links) whose path argument touches the directory being moved, same as `src/lib/theme/blog.ts` and `src/lib/server/content.ts` do for their own sections.
- **Before redoing or "fixing" something the user says is missing or wrong, verify against the actual current file/browser state first, not memory of what was intended.** A real case: after a large batch, the user said the new patterns were "forgotten" from the Pattern Index; a grep of `src/routes/patterns/+page.md` showed all six were already there from the original batch, and a live dev-server check confirmed it. The claim was wrong, not the work — spending one grep/read confirming this before touching anything avoided a pointless duplicate edit (and duplicate edits to markdown-in-`.md` component tags are exactly the kind of thing that can silently corrupt formatting, see the nested-component gotchas below). This is the same discipline as the ticket-verification rule immediately below, just applied to a spoken claim about repo state instead of a written ticket's content claims.
- **When a new content catalog needs its own index-card and detail-card components (Retrofit Strategies mirroring Patterns is the precedent so far), mirror the sibling component's mechanics exactly (image treatment, number-badge mechanism, hover states, shared layout utility class) rather than inventing new ones, and derive the new component's coloring from whichever existing classification is its closest functional/semantic peer instead of a separate palette.** Concretely: `RetrofitIndexCard`/`RetrofitCard` reuse `PatternIndexCard`/`PatternCard`'s exact shell and the `.pattern-index-grid` layout class, and Retrofit Strategies borrow Pattern classification colors (Intersection-scale strategies get the real Intersections & Crossings ramp, since Intersection patterns already have one; Corridor/Network strategies share Patterns' own still-placeholder Freeway-gray). Apply this same reasoning to the next new catalog type this site adds, rather than proposing a new palette from scratch.
- **When the user hands over a written ticket/instruction file (a spec drafted outside a live look at the repo), verify its concrete claims against the actual code rather than transcribing them literally** — this repo has now hit two real cases: a ticket describing citations as raw `<sup class="citation"><a href="#ref-N">` HTML when every page on the site actually uses the `<Citation index={N} />` component (translated silently, since it's clearly an authoring-convention gap, not a content decision); and a ticket specifying a badge color as a literal hex it described as "reusing the existing `--cw-primary-soft` token," where that token's real value produced only 1.99:1 contrast in dark mode (fixed with a proper theme-aware token, see `--cw-descriptive-ink` in `tokens.css`, rather than shipping the literal hex). Silently translate pure authoring-syntax mismatches to the site's real conventions; verify-then-fix (not just flag) a literal value that turns out to fail contrast or another concrete correctness check; still flag actual content/design judgment calls (a ticket reversing a documented convention, like Ticket B's intersection status badges, was confirmed with the user before proceeding, not decided unilaterally either way).
